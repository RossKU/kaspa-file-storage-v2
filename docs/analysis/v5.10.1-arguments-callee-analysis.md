# arguments.callee 修正の徹底分析

## 🔍 現在の実装分析

### 1. 問題のコード構造

```javascript
// 11914行目から
AppState.monitoring.wsMonitorTimeout = setTimeout(async () => {
    // アップロード中チェック
    if (FileState.uploadSessionActive || /* 他の条件 */) {
        // タイマーリセット（問題箇所）
        clearTimeout(AppState.monitoring.wsMonitorTimeout);
        AppState.monitoring.wsMonitorTimeout = setTimeout(arguments.callee, 5 * 60 * 1000);
        return;
    }
    // トランザクション監視中チェック
    if (NetworkState.ws.monitoredTransactions.size > 0) {
        // タイマーリセット（問題箇所）
        clearTimeout(AppState.monitoring.wsMonitorTimeout);
        AppState.monitoring.wsMonitorTimeout = setTimeout(arguments.callee, 5 * 60 * 1000);
        return;
    }
    // 5分経過で自動停止
    await stopMonitoring();
}, 5 * 60 * 1000);
```

### 2. 実行フロー

1. `window.startMonitoring()` が呼ばれる
2. 5分タイマーがセットされる
3. 5分後にタイマー関数が実行される
4. アップロード中なら `arguments.callee` で自己参照して再度5分タイマーをセット
5. アップロードが完了したら `stopMonitoring()` を呼ぶ

### 3. 関連する状態管理

- `FileState.uploadSessionActive`: アップロードセッション中フラグ
- `FileState.isUploadingFile`: ファイルアップロード中フラグ
- `FileState.uploadingChunksCount`: アップロード中のチャンク数
- `FileState.uploadingMetaTx`: メタデータトランザクション中フラグ
- `uploadingDirMeta`: ディレクトリメタデータアップロード中フラグ
- `NetworkState.ws.monitoredTransactions`: 監視中のトランザクション

### 4. エラー時の影響

**現在の実装でTypeErrorが発生した場合**:
1. タイマーのリセットが失敗
2. 新しいタイマーがセットされない
3. 既存のタイマーは5分後に実行される
4. アップロード中でも `stopMonitoring()` が呼ばれる
5. **WebSocket監視が停止し、アップロード監視が失われる**

## 🔧 修正案の検証

### 修正案1: 名前付き関数式

```javascript
AppState.monitoring.wsMonitorTimeout = setTimeout(async function checkTimeout() {
    if (FileState.uploadSessionActive || /* 他の条件 */) {
        clearTimeout(AppState.monitoring.wsMonitorTimeout);
        AppState.monitoring.wsMonitorTimeout = setTimeout(checkTimeout, 5 * 60 * 1000);
        return;
    }
    // 以下同様
}, 5 * 60 * 1000);
```

**メリット**:
- シンプルで分かりやすい
- 関数名で意図が明確
- エラーが発生しない

**デメリット**:
- なし

### 修正案2: 外部関数として定義

```javascript
async function wsMonitorTimeoutHandler() {
    if (FileState.uploadSessionActive || /* 他の条件 */) {
        clearTimeout(AppState.monitoring.wsMonitorTimeout);
        AppState.monitoring.wsMonitorTimeout = setTimeout(wsMonitorTimeoutHandler, 5 * 60 * 1000);
        return;
    }
    // 以下同様
}

AppState.monitoring.wsMonitorTimeout = setTimeout(wsMonitorTimeoutHandler, 5 * 60 * 1000);
```

**メリット**:
- 関数が再利用可能
- テストしやすい

**デメリット**:
- スコープが広がる（グローバル汚染の可能性）

### 修正案3: アロー関数の変数化

```javascript
const timeoutHandler = async () => {
    if (FileState.uploadSessionActive || /* 他の条件 */) {
        clearTimeout(AppState.monitoring.wsMonitorTimeout);
        AppState.monitoring.wsMonitorTimeout = setTimeout(timeoutHandler, 5 * 60 * 1000);
        return;
    }
    // 以下同様
};

AppState.monitoring.wsMonitorTimeout = setTimeout(timeoutHandler, 5 * 60 * 1000);
```

**メリット**:
- モダンな記法
- スコープが限定的

**デメリット**:
- 関数定義と使用が分離

## 🎯 推奨する修正方法

**修正案1（名前付き関数式）が最適**

理由:
1. 既存のコード構造を最小限の変更で修正
2. 関数のスコープが限定的
3. エラーが発生しない
4. デバッグ時に関数名が表示される

## ✅ 修正後の動作確認ポイント

1. **通常のアップロード**
   - 5分以上かかるファイルのアップロード
   - タイマーが正しくリセットされること

2. **エラー発生時**
   - ネットワークエラー時も監視が継続すること
   - RPC再接続時の動作

3. **監視停止条件**
   - アップロード完了後に正しく停止すること
   - 手動停止（`stopMonitoring()`）が機能すること

4. **メモリリーク**
   - タイマーが適切にクリアされること
   - イベントリスナーが解放されること

## 🚨 注意事項

1. `uploadingDirMeta` の参照は `FileState.uploadingDirMeta` に統一することを推奨
2. エラーハンドリングは既存のまま（try-catchブロック内）で問題なし
3. タイマーIDの管理（`AppState.monitoring.wsMonitorTimeout`）は変更不要
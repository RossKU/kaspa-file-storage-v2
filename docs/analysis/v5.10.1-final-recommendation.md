# v5.10.1 arguments.callee修正 最終推奨事項

## ✅ 徹底分析の結論

### 修正は安全に実行可能

**理由**:
1. **影響範囲が限定的**: タイマーコールバック内のみの変更
2. **エラーハンドリング済み**: 既存のtry-catchブロックで保護されている
3. **状態管理に影響なし**: FileState、NetworkState、AppStateへの参照は変更なし
4. **クロージャが正常動作**: 名前付き関数式は外部変数にアクセス可能

## 🔧 推奨する修正内容

```javascript
// 11914行目から修正
AppState.monitoring.wsMonitorTimeout = setTimeout(async function checkTimeout() {
    // v5.3.7: Enhanced check - Don't stop monitoring during any upload activity
    if (FileState.uploadSessionActive || FileState.isUploadingFile || FileState.uploadingChunksCount > 0 || FileState.uploadingMetaTx || FileState.uploadingDirMeta) {
        log(`[DEBUG] アップロード中 - WebSocket監視を継続 (session=${FileState.uploadSessionActive}, file=${FileState.isUploadingFile}, chunks=${FileState.uploadingChunksCount})`, 'debug');
        // Reset timeout
        clearTimeout(AppState.monitoring.wsMonitorTimeout);
        AppState.monitoring.wsMonitorTimeout = setTimeout(checkTimeout, 5 * 60 * 1000);
        return;
    }
    
    // Also check if there are monitored transactions
    if (NetworkState.ws.monitoredTransactions.size > 0) {
        log(`[DEBUG] 監視中のトランザクションあり (${NetworkState.ws.monitoredTransactions.size}件) - 監視を継続`, 'debug');
        clearTimeout(AppState.monitoring.wsMonitorTimeout);
        AppState.monitoring.wsMonitorTimeout = setTimeout(checkTimeout, 5 * 60 * 1000);
        return;
    }
    
    log('WebSocket監視: 5分タイムアウト - 自動停止', 'warning');
    await stopMonitoring();
}, 5 * 60 * 1000);
```

## 🛡️ 修正の安全性確認

### 1. **アップロード継続性**
- ✅ アップロード中の監視は正常に継続
- ✅ タイマーリセットは正常動作
- ✅ エラー時もアップロードは中断されない

### 2. **メモリ管理**
- ✅ clearTimeoutで前のタイマーを適切にクリア
- ✅ stopMonitoringでも適切にクリーンアップ
- ✅ メモリリークの心配なし

### 3. **エラー処理**
- ✅ 既存のtry-catchブロックで保護
- ✅ TypeErrorが解消される
- ✅ 他のエラーは既存通り処理

### 4. **副作用**
- ✅ 他の機能への影響なし
- ✅ グローバルスコープ汚染なし
- ✅ 既存の動作を完全に維持

## 📋 修正手順

1. **ファイルコピー**
   ```bash
   cp kaspa-storage-webui-v5.10.0.html kaspa-storage-webui-v5.10.1.html
   ```

2. **修正適用**
   - 11914行目: `setTimeout(async () => {` → `setTimeout(async function checkTimeout() {`
   - 11920行目: `setTimeout(arguments.callee,` → `setTimeout(checkTimeout,`
   - 11928行目: `setTimeout(arguments.callee,` → `setTimeout(checkTimeout,`

3. **追加推奨修正**
   - 11916行目: `uploadingDirMeta` → `FileState.uploadingDirMeta` （一貫性のため）

4. **バージョン更新**
   - タイトルを v5.10.1 に変更

## 🧪 動作確認ポイント

1. **5分以上のアップロード**
   - 大きなファイルをアップロード
   - 5分経過後も監視が継続することを確認

2. **コンソールログ確認**
   - TypeErrorが発生しないこと
   - 「アップロード中 - WebSocket監視を継続」ログが表示

3. **正常な監視停止**
   - アップロード完了後に監視が停止
   - 手動停止も正常動作

## ⚠️ 注意事項

- **必ずバックアップを取る**（v5.10.0を残す）
- **開発者ツールでエラーを監視**
- **実際のファイルアップロードでテスト**

## 🎯 結論

この修正は**完全に安全**であり、実装を推奨します。TypeErrorを解消し、アップロード監視の継続性を保証します。
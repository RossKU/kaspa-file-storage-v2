# v5.10.6 ディレクトリメタトランザクション復号化エラー分析

## 🔍 エラー概要

**エラーメッセージ**: 「これは有効なメタトランザクションではありません」

**発生状況**:
- ディレクトリメタデータを直接ブロックチェーンにアップロード
- TxID:BlockID:Password形式で入力
- 復号化は成功し、`{"type":"kaspa-directory","version":"3.4","created...`が得られた
- しかし、エラーが発生

## 💡 原因分析

### ログからの情報

```
21:48:35 - ペイロード取得成功: 1096 bytes
21:48:35 - Decrypted string preview: {"type":"kaspa-directory","version":"3.4","created...
21:48:35 - 復号化エラー: これは有効なメタトランザクションではありません
```

### コード分析

**問題箇所**（kaspa-storage-webui-v5.10.6.html:11036-11037）
```javascript
if ((metaData.version === "2.1" || metaData.version === "3.3" || metaData.version === "3.1.1") && 
    (metaData.type === "kaspa-meta-tx" || metaData.type === "kaspa-directory")) {
```

**問題点**: 条件文に`version === "3.4"`が含まれていない

## ✅ 解決方法

### 修正コード

```javascript
if ((metaData.version === "2.1" || metaData.version === "3.3" || metaData.version === "3.1.1" || metaData.version === "3.4") && 
    (metaData.type === "kaspa-meta-tx" || metaData.type === "kaspa-directory")) {
```

### 修正理由

1. v3.4.0仕槕が新たに策定された
2. ディレクトリメタデータはv3.4形式で保存された
3. コードがv3.4への対応が漏れていた

## 📄 関連情報

### 既存のディレクトリ処理

コードには既にディレクトリタイプの処理が実装されている：

```javascript
// ディレクトリタイプの場合
if (metaData.type === 'kaspa-directory') {
    log('ディレクトリメタデータを検出しました', 'info');
    
    // ディレクトリメタデータとして処理
    const dirMetadata = {
        ...metaData,
        auth: {
            passwordIncluded: true,
            password: password
        }
    };
    
    await loadDirectoryMetadata(dirMetadata);
    document.getElementById('downloadSection').style.display = 'block';
    return;
}
```

### 成功ケース

- `.kaspa`ファイルからのディレクトリ読み込みは成功
- バージョンチェックがないため、`.kaspa`ファイル読み込みではv3.4でも動作

## 🛠️ 修正手順

1. kaspa-storage-webui-v5.10.6.htmlの11036行目を編集
2. `metaData.version === "3.4"`を条件に追加
3. テストしてデプロイ

## 📝 まとめ

- エラーの原因はv3.4バージョンのサポート漏れ
- ディレクトリ処理自体は既に実装済み
- 1行の修正で解決可能

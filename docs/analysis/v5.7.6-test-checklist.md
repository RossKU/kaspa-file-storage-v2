# v5.7.6 テストチェックリスト

## テスト概要
v5.7.6では、handleError関数の条件問題を修正し、RPCクライアントが確実に破棄されるようにしました。

## 重要な確認ポイント

### 1. handleErrorのログ確認

#### v5.7.5で出なかったログが出るようになること
```
[ConnectionManager] ネットワークエラーを検出
[ConnectionManager] RPC接続を切断しました  ← v5.7.6で出るようになる
[ConnectionManager] RPCクライアントをクリアしました  ← v5.7.6で出るようになる
```

### 2. 二重破棄の防止確認

#### シナリオ
1. WebSocket切断イベント → RPCクライアントをnull
2. その後handleError呼び出し → 既にnullなのでスキップ

#### 期待される動作
- エラーが発生しない
- 「RPC切断エラー」のログが出ても正常

### 3. 再接続ループの改善確認

#### v5.7.5のパターン
```
切断 → 再接続（警告:残存） → 切断 → 再接続（警告:残存）...
```

#### v5.7.6で期待されるパターン
```
切断 → handleErrorでクリア → 再接続（残存なし） → 安定
```

## テスト手順

### 1. 基本動作確認
1. https://rossku.github.io/kaspa-file-storage-v2/kaspa-storage-webui-v5.7.6.html にアクセス
2. デバッグモードを有効化
3. ファイルアップロードを開始

### 2. ネットワーク切断テスト
1. アップロード中にネットワークを切断
2. 以下のログシーケンスを確認：
   ```
   [WebSocket] 切断イベントを検出
   [WebSocket] RPCクライアントをクリアしました
   [ConnectionManager] ネットワークエラーを検出
   [ConnectionManager] RPC接続を切断しました  ← NEW!
   [ConnectionManager] RPCクライアントをクリアしました  ← NEW!
   ```

### 3. 再接続の安定性確認
1. ネットワークを復活させる
2. 再接続時のログを確認：
   ```
   [ConnectionManager] 新規RPCクライアントを作成します
   ```
   - 「警告: RPCクライアントが残存していました」が出ないことが理想
   - 出ても自己修復されれば問題なし

### 4. ループ問題の確認
- 4秒間隔の切断ループが改善されているか
- より安定した再接続パターンになっているか

## 成功の判断基準

1. ✅ handleErrorでRPCクライアント破棄ログが出る
2. ✅ 二重破棄でエラーが発生しない
3. ✅ 再接続がより安定している
4. ✅ 「Attempt to use a disposed resource」エラーが出ない（v5.7.5と同様）

## 注意事項

### ループ問題について
v5.7.6は条件問題の修正に焦点を当てています。再接続タイミングの最適化（遅延時間の調整など）は将来の課題として残っています。

しかし、RPCクライアントの確実な破棄により、再接続の安定性は向上するはずです。
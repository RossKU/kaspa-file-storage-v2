# v5.7.7 テストチェックリスト

## 概要
v5.7.7では、v5.7.6で発生した再接続不能の問題を修正しました。

## 重要な確認ポイント

### 1. 再接続機能の復旧確認
- [ ] ページをロードして初回接続が成功すること
- [ ] ネットワークを一時的に切断して、自動再接続が動作すること
- [ ] 再接続成功のログが表示されること

### 2. エラーログの確認

#### 修正されたエラー（出なくなること）
```
RPC初期化エラー: updateNodeConnectionStatus is not defined
[ConnectionManager] 再接続失敗: Cannot read properties of null (reading 'getServerInfo')
```

#### 新しいログ（出ること）
```
[ConnectionManager] 既に再接続中です
```
（複数の切断イベントが発生した場合のみ）

### 3. 再接続の流れ確認
1. WebSocket切断
2. 2秒後に再接続試行
3. 成功または失敗のログ
4. 失敗時は指数バックオフで再試行

### 4. 並行処理の確認
- [ ] ネットワークを素早く切断・接続を繰り返しても問題ないこと
- [ ] 「既に再接続中です」のログが適切に出ること

## デバッグ情報の確認方法

### コンソールで確認すべきログ
```javascript
// 再接続の開始
[ConnectionManager] 再接続試行 #1

// 正常な再接続
[ConnectionManager] 新規RPCクライアントを作成します
[ConnectionManager] RPC接続成功: 1.0.0

// エラー時
[ConnectionManager] 再接続失敗: [エラーメッセージ]
```

### 状態の確認
```javascript
// コンソールで実行
console.log('RPC Client:', NetworkState.rpcClient ? 'exists' : 'null');
console.log('Is Reconnecting:', ConnectionManager.isReconnecting);
console.log('Is Connected:', ConnectionManager.isConnected);
```

## 期待される動作

1. **初回接続**: 正常に接続
2. **切断時**: 自動的に再接続を試行
3. **再接続成功**: アップロードやダウンロードが再開可能
4. **複数エラー**: 重複した再接続試行を防止

## 注意事項
- v5.7.6では完全に再接続できなかったが、v5.7.7では確実に再接続できるはず
- updateNodeConnectionStatusエラーは出なくなる
- null参照エラーも出なくなる
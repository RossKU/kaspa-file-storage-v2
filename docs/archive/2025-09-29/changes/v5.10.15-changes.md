# v5.10.15 変更内容

## 🌳 概要
エクスプローラー型ツリービュー機能を実装しました。選択したフォルダにファイルやディレクトリを追加できます。

## 🔧 変更内容

### UI変更
- **TxID入力欄を常時表示**: 個別のボタンを廃止し、統一された入力欄に
- **ツリービュー追加**: ディレクトリ構造を階層的に表示
- **選択状態の視覚化**: 選択中のフォルダをハイライト表示

### DirectoryBuilderStateの拡張
```javascript
// 新規プロパティ
currentPath: '',      // 現在選択中のパス
selectedNode: null,   // 選択中のノード

// 新規メソッド
addFromTxId()        // TxIDから自動判別して追加
buildTreeStructure() // フラットな配列からツリー構造を構築
renderTree()         // ツリービューをレンダリング
selectNode()         // ノードを選択
```

### 機能詳細

#### 自動メタデータ判別
- `kaspa-file`またはスーパーメタ → ファイルとして追加
- `kaspa-directory` → ディレクトリとして追加
- ディレクトリの中身も展開オプション付き

#### ツリー表示
```
📁 My Directory
├─ 📁 folder1 [選択中]
│  ├─ 📄 file1.txt (12.5KB)
│  └─ 📄 file2.pdf (234.1KB)
│  └─ [ここに追加...]
├─ 📁 folder2
└─ 📄 root-file.doc (45.6KB)
```

#### パス管理
- フォルダクリックで選択変更
- 選択したフォルダのパスに新規エントリーを追加
- 空のフォルダは自動的に作成

## 🎯 使い方
1. TxID:BlockID:Password を入力欄に入力
2. 「追加」ボタンでメタデータを取得・判別
3. ツリービューでフォルダを選択
4. 選択したフォルダに自動的に追加

## 📝 技術的詳細
- `downloadFromTxId`を利用してメタデータを取得
- フラットなエントリー配列から階層構造を動的生成
- パス文字列を解析して中間フォルダを自動作成
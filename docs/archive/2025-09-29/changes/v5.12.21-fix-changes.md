# v5.12.21-fix: 最小幅と表示改善

## 問題
1. カラムに最小幅がなく、画面が狭いと潰れる
2. 容量カラムが80pxでは「MB」などが表示しきれない
3. フォルダの場合もファイルサイズが表示される
4. 日付が「今日」「昨日」などで不明確

## 修正内容

### 1. 最小幅（min-width）を追加（1263-1305行目）
各カラムに適切な最小幅を設定：
| カラム | width | min-width | max-width |
|--------|-------|-----------|-----------|
| チェックボックス | 40px | **40px** | - |
| アイコン | 50px | **50px** | - |
| ファイル名 | 250px | **100px** | 250px |
| 容量 | **100px** | **100px** | - |
| CID | 150px | **80px** | 150px |
| MetaTxID | 150px | **80px** | 150px |
| アップローダー | 180px | **100px** | 180px |
| UP日 | **120px** | **120px** | - |
| 登録日 | **120px** | **120px** | - |

### 2. 容量カラムの拡張（1279-1282行目）
- 80px → **100px**に拡張
- 「2.5 MB」などが切れずに表示可能

### 3. フォルダのファイル数表示（17525-17544行目）
```javascript
if (entry.type === 'kaspa-directory') {
    // フォルダの場合はファイル数を表示
    return `${fileCount} files${dirCount > 0 ? `, ${dirCount} dirs` : ''}`;
}
```
- フォルダ：「15 files, 3 dirs」
- ファイル：従来通りサイズ表示

### 4. 日付フォーマット変更（17600-17610行目）
```javascript
// 変更前：「今日」「昨日」「3日前」
// 変更後：「2025/01/08 14:30」
return `${year}/${month}/${day} ${hours}:${minutes}`;
```

## 効果
- ✅ **最小幅保証**：画面が狭くても最低限の表示を維持
- ✅ **容量表示改善**：MBやGBが切れない
- ✅ **フォルダ情報**：ファイル数とディレクトリ数を表示
- ✅ **明確な日時**：正確な年月日時分を表示

## 技術詳細
- 合計最小幅：790px（すべてのカラム表示時）
- 日付カラム幅：100px → 120px（YYYY/MM/DD HH:MM形式対応）
- フォルダ判定：`entry.type === 'kaspa-directory'`
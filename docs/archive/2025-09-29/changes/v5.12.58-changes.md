# v5.12.58: フルCID保存修正

## 問題
- 短縮版CID（8文字）が保存されていた
- CSVからCIDが読み込まれていなかった
- ファイルサイズが0と表示される

## 原因
1. **保存時の問題（13380行目）**:
   - `window.currentFileCID`（短縮版）を保存
   - `window.currentFileCIDFull`（フル版）を使うべき

2. **CSV読み込み時の問題（4705行目）**:
   - カラム25（CID）のマッピングが欠落
   - カラム15（fileSize）を`size`プロパティにも設定する必要

## 修正内容

### 1. フルCIDを保存（13380行目）
```javascript
// 修正前
kenvEntry.cid = window.currentFileCID || '';

// 修正後
kenvEntry.cid = window.currentFileCIDFull || window.currentFileCID || '';
```

### 2. CIDカラムマッピング追加（4706行目）
```javascript
if (values[25]) entry.cid = values[25];  // v5.12.58: CIDマッピング追加
```

### 3. サイズプロパティの互換性（4702-4705行目）
```javascript
if (values[15]) {
    entry.fileSize = parseInt(values[15]) || null;
    entry.size = entry.fileSize;  // v5.12.58: sizeプロパティも設定（互換性のため）
}
```

## 効果
- フルCID（43文字）が正しく保存される
- CSVからCIDが正しく読み込まれる
- ファイルサイズが正しく表示される
- Infoボタンで完全な情報が表示される

## CID構造
- **フルCID**: 43文字のBase64エンコード（SHA-256ハッシュ）
- **短縮版CID**: 最初の8文字（表示用）
- **生成方法**: ファイル先頭4KB + ファイルサイズ → SHA-256 → Base64
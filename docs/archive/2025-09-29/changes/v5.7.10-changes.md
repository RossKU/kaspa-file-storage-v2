# v5.7.10 変更内容

## 修正内容
processAndUploadの2重実行を防止し、トランザクション重複送信を解決しました。

### 主な変更点

#### 1. attemptReconnect呼び出しの削除
```javascript
// v5.7.10: Don't call attemptReconnect to prevent dual execution
if (!ConnectionManager.isConnected) {
    log(`[DEBUG] ネットワーク切断 - 自動再開に任せます`, 'debug');
    throw new Error('Network disconnected - waiting for auto-resume');
}
```

### 問題の原因
v5.7.9でセッション維持が実現した結果、以下の状況が発生：
1. エラー時のprocessAndUploadが`attemptReconnect()`で再接続を待機
2. ConnectionManagerが再接続後に新しい`processAndUpload()`を自動開始
3. 2つのprocessAndUploadが同時実行され、同じペイロードを重複送信

### 解決方法
`attemptReconnect()`を呼ばずにエラーをthrowすることで、元のprocessAndUploadを終了させ、ConnectionManagerの自動再開のみに任せる。

## 技術的詳細

### 修正前の動作
- ネットワークエラー時に再接続を待って処理継続
- 自動再開も同時に動作
- 結果：2重実行

### 修正後の動作
- ネットワークエラー時は処理を中断
- ConnectionManagerの自動再開のみが動作
- 結果：単一実行

## 影響範囲
- 修正箇所：1箇所（8298-8300行目）
- 変更行数：2行
- 既存機能への影響：なし

## テスト項目
1. 通常のアップロード動作
2. ネットワーク切断時の処理中断
3. 自動再開による継続
4. ペイロード重複送信がないこと
5. UI表示の重複がないこと

## リスク評価
- **低リスク**: エラー処理の変更のみ
- **副作用**: なし
- **後方互換性**: 完全に維持

## 期待される効果
1. トランザクション重複送信の防止
2. コスト削減（無駄な送信の排除）
3. UI表示の正常化
4. 進捗管理の整合性向上
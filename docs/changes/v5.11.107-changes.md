# v5.11.107 変更内容 - ディレクトリメタデータv3.4.2の認識エラー修正

## 実施日時
2025-08-03

## 変更内容
loadFromMetaTxId関数のバージョンチェックにv3.4.2を追加

## 背景
- DirectoryBuilderで作成したディレクトリメタデータ（v3.4.2）が復元できない
- 「これは有効なメタトランザクションではありません」エラーが発生
- 原因：バージョンチェックリストにv3.4.2が含まれていなかった

## 修正内容
14378行のバージョンチェックを修正：
```javascript
// 修正前
if ((metaData.version === "2.1" || metaData.version === "3.3" || metaData.version === "3.1.1" || metaData.version === "3.4") && 

// 修正後
if ((metaData.version === "2.1" || metaData.version === "3.3" || metaData.version === "3.1.1" || metaData.version === "3.4" || metaData.version === "3.4.2") && 
```

## 結果
- v3.4.2のディレクトリメタデータが正しく認識される
- DirectoryBuilderで作成したディレクトリが復元可能に
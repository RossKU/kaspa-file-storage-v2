# v5.11.109 変更内容 - DirectoryBuilder KENV連携実装

## 実施日時
2025-08-03

## 変更内容

### Phase 1: saveToKENVメソッドの追加
DirectoryBuilderStateにKENV保存機能を追加

### 背景
- 「生成のみ」ボタンが自動的に.kaspaファイルをダウンロードしていた
- KENVへの保存機能がなかった
- ユーザーの期待する動作（生成→KENV保存→後で共有）と異なっていた

### 実装内容
1. **saveToKENVメソッドの追加** (5203行後)
   - 生成されたディレクトリメタデータをKENVエントリーとして保存
   - ディレクトリ固有の情報（directoryMetadata、password、cid）を記録
   - File Tabの自動更新

2. **generateOnlyメソッドの修正** (5239行)
   - exportAsKaspa()の自動呼び出しを削除
   - saveToKENV()の呼び出しに変更

### 期待される動作
1. 「生成のみ」ボタン → メタデータ生成 → KENV保存
2. File Tabでディレクトリエントリーを確認可能
3. 共有メニューから.kaspaファイル生成可能

### 次のフェーズ
- Phase 2: KENVManagerのgenerateKaspaFileメソッドをディレクトリタイプに対応
- Phase 3: generateAndUpload後のKENVエントリー更新
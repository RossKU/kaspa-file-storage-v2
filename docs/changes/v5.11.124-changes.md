# v5.11.124: メインステータスタブのアーカイブノード個別表示（修正版）

## 変更内容

### 1. v5.11.123の問題を修正
- **問題**: `node.type === 'full'`の条件が機能していなかった
- **原因**: 実際のデータは`type: 'threshold-based'`として保存されていた
- **修正**: `node.blockCount`の存在のみを条件に変更

### 2. 正しい実装（10093-10114行）
```javascript
// v5.11.124: Show individual node details with blocks and days
let detailsHtml = '';
stats.nodes.forEach((node, idx) => {
    if (node.blockCount) {  // typeの条件を削除
        const blocks = (node.blockCount / 1000000).toFixed(1);
        const days = calculateDaysFromBlocks(node.blockCount);
        const status = node.healthy ? '✅' : '⚠️';
        detailsHtml += `N${idx+1}: ${blocks}M (${days}days+) | ${node.avgResponseTime.toFixed(0)}ms ${status}<br>`;
    }
});
```

## 表示例

```
Archive Node: Connected
N1: 217.6M (251.9days+) | 45ms ✅
N2: 215.3M (249.2days+) | 52ms ✅
N3: 217.6M (251.9days+) | 38ms ✅
N4: 210.1M (243.2days+) | 61ms ✅
N5: 205.8M (238.2days+) | 47ms ✅
接続数: 5/5 (健全: 5)
```

## v5.11.123からの変更点

- Gitでv5.11.122にロールバック後、正しく実装し直し
- typeチェックを削除し、blockCountの存在のみで判定
- これにより全てのアーカイブノードの情報が表示されるように修正
# v5.11.15 変更履歴

## リファクタリング
- `PROCESS_CHUNK_SIZE`を`PROCESS_SEGMENT_SIZE`にリネーム
  - より正確な命名に変更（実際にはセグメント処理に使用）
  - 4箇所を修正:
    - グローバル定義（2435行目）
    - アップロード処理内の定義（10193行目）
    - ファイル処理ループ（10231行目）
    - インデックス計算（10240行目、10246行目）
  - `chunkIndex`も`segmentIndex`に変更して一貫性を保つ
  - 注: 8115行目の128KBの`PROCESS_CHUNK_SIZE`は別物なので変更せず

## 技術的詳細
- 1MBのセグメント単位でファイルを処理
- 各セグメントは複数のペイロード（チャンク）に分割される
- この変更により、コード内の命名がより実装に即したものになった
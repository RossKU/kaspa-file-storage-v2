# v5.11.66 変更内容 - KENV完全データ保存実装

## 実装日時
2025-07-31

## 概要
KENVエントリーにFileState.kaspaMetadataの完全なデータを保存する機能を実装しました。これにより、.kaspaファイルの生成時に必要なすべての情報が利用可能になり、ダウンロードが正しく動作するようになります。

## 技術的変更内容

### 1. アップロード完了時のKENVエントリー作成処理（13368行目～）
- salt（暗号化ソルト）の保存を追加
- 圧縮情報の詳細（algorithm、enabled、originalSize、compressedSize、variableChunk）を保存
- chunks配列とchunkBlockIds配列を内部プロパティ（_chunks、_chunkBlockIds）として保存
- segmentBoundariesを内部プロパティ（_segmentBoundaries）として保存

### 2. saveKentryFile関数（3625行目～）
- 固定45カラムの後に可変カラムを追加する処理を実装
- チャンクペア（tx1,blk1,tx2,blk2,...）の保存
- セグメントバウンダリー（seg,index,offset,size,originalSize,compressedSize,payloadIndex）の保存

### 3. loadKentryFile関数（3854行目～）
- 45カラム以降の可変カラムを正しく読み取る処理を実装
- 'seg'マーカーを使用してセグメントバウンダリーとチャンクペアを識別
- 内部プロパティとしてデータを復元

### 4. generateKaspaFromKENV関数（17868行目、17890行目、17909行目）
- 空のデータの代わりに、保存されたデータを使用：
  - salt: kenvEntry.salt || ""
  - chunks: kenvEntry._chunks || []
  - chunkBlockIds: kenvEntry._chunkBlockIds || []
  - segmentBoundaries: kenvEntry._segmentBoundaries || []

## 影響と利点

1. **完全なデータ保存**: FileState.kaspaMetadataのすべての重要情報がKENVに保存される
2. **正確な.kaspaファイル生成**: 必要なすべての情報が含まれた.kaspaファイルが生成可能
3. **ダウンロード機能の修復**: chunks配列とsaltが正しく保存されるため、ダウンロードが動作する
4. **PayloadSplit対応**: セグメントバウンダリーが保存されるため、PayloadSplit使用時も正確に復号化可能

## KENVフォーマットv3.1.3準拠
- 固定45カラム + 可変カラム構造を正しく実装
- チャンクペアとセグメントバウンダリーの柔軟な格納

## 検証方法
1. ファイルをアップロード
2. Fileタブで.kaspaファイルを生成
3. 生成された.kaspaファイルにchunks、salt、segmentBoundariesが含まれることを確認
4. .kaspaファイルを使用してダウンロードが成功することを確認
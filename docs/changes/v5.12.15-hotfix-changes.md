# v5.12.15-hotfix: clearFileSelection()呼び出し削除

## 問題
- v5.12.15でclearFileSelection()を呼ぶようにしたが、進捗ファイル（.kprogress）のレジューム表示も消えてしまう
- サムネイルは表示されるが、進捗ウィンドウが消える

## 原因
- clearFileSelection()はファイル選択UIを完全にリセットする
- 進捗ファイルのレジューム表示も含めて全てクリアしてしまう

## 修正内容（13027-13028行目）
```javascript
// 修正前
if (typeof clearFileSelection === 'function') {
    clearFileSelection();  // 進捗UIも消してしまう
}

// 修正後  
// v5.12.15: Do not call clearFileSelection() here
// It would clear the progress file UI which we want to keep
```

## 効果
- FileStateのクリア（13024-13025行）は維持される
- HTML構造が保持される（v5.11.135のinnerHTML問題は既に削除済み）
- 進捗ファイルのレジューム表示が維持される
- サムネイル表示も正常に動作する

## 技術詳細
アップロード完了後の処理：
1. FileState.currentFile = null ✅（ファイル状態クリア）
2. FileState.selectedFiles.clear() ✅（選択状態クリア）
3. clearFileSelection()呼び出し ❌（削除）

これにより、最小限の状態リセットのみ行い、UIは維持される。
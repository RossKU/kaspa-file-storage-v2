# v5.12.51-hotfix: KENV重複チェック修正

## 修正内容
前バージョンで実装した重複チェックロジックを正しく修正

## 削除した誤った条件
- ❌ 同じIDでの重複判定（不要）
- ❌ 5分以内という時間条件（勝手に追加していた）

## 正しい重複判定基準

### ファイルタイプ（kaspa-file）
- **MetaTxIDがある場合**: 名前 + CID + MetaTxID
- **MetaTxIDがない場合**: 名前 + CID + 最初のチャンクTxID（_chunks[0]）

### ディレクトリタイプ（kaspa-directory）
- **CIDとTxIDがある場合**: 名前 + CID + MetaTxID
- **TxIDがない場合**: 名前 + CID + ファイル数 + ディレクトリ数 + 作成日

## 実装詳細
- 変更箇所：5082-5149行目（mergeTemporaryData関数）
- タイプ別に適切な重複チェックロジックを実装
- 別セッションの同じファイルは履歴として保持

## この修正により
- ユーザーの意図通りの重複判定が実現
- アップロード履歴を適切に保護
- 完全に同じエントリーのみをスキップ
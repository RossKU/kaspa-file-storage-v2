# v5.7.11 変更内容

## 修正内容
networkErrorOccurredフラグのクリアタイミングを修正し、ProgressManagerが正しく保持されるようにしました。

### 主な変更点

#### 1. フラグクリア処理の移動
**修正前（v5.7.10）**の問題のある順序：
```javascript
} finally {
    // 1. セッション維持判定
    if (!FileState.networkErrorOccurred) {
        FileState.uploadSessionActive = false;
    }
    
    // 2. フラグクリア（早すぎる！）
    if (FileState.networkErrorOccurred) {
        FileState.networkErrorOccurred = false;  // ← ここで消してしまう
    }
    
    // 3. ProgressManager判定（フラグがfalseになっているため破棄される）
    if (!FileState.networkErrorOccurred) {
        FileState.progressManager = null;  // ← 常に実行される！
    }
}
```

**修正後（v5.7.11）**の正しい順序：
```javascript
} finally {
    // 1. セッション維持判定
    if (!FileState.networkErrorOccurred) {
        FileState.uploadSessionActive = false;
    }
    
    // 2. ProgressManager判定（フラグがtrueのまま → 正しく保持される）
    if (!FileState.networkErrorOccurred) {
        FileState.progressManager = null;
    } else {
        log('ネットワークエラーのため、ProgressManagerを保持します', 'info');
    }
    
    // 3. フラグクリア（最後に移動！）
    if (FileState.networkErrorOccurred) {
        FileState.networkErrorOccurred = false;
    }
}
```

### 実装詳細
- **修正箇所**: finallyブロック内のコード順序
- **修正行数**: 約5行（コードブロックの移動のみ）
- **ロジック変更**: なし（実行順序の変更のみ）

## 解決される問題
1. **ProgressManagerの不適切な破棄**
   - ネットワークエラー時でもProgressManagerが破棄されていた問題を修正
   
2. **自動再開の失敗**
   - ProgressManagerが存在しないため、自動再開条件を満たさなかった問題を解決
   
3. **セッション継続の完全性**
   - v5.7.9で実現したセッション維持が、v5.7.10でも不完全だった部分を修正

## テスト済みシナリオ
- 通常のアップロード
- ネットワーク切断時の動作
- 再接続後の自動再開
- ProgressManagerの保持確認

## 注意事項
- この修正はv5.7.9、v5.7.10の改善を基盤としています
- 既存の機能に影響を与えません
- 最小限の変更で最大の効果を実現しています
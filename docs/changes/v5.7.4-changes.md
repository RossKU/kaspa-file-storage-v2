# v5.7.4 変更内容

## 修正内容
v5.7.1で追加された回避策コードを削除し、コードをクリーンアップしました。

### 変更箇所
1. **disconnectイベントハンドラー（11364-11367行目）**
   - monitorActiveチェックを削除
   - v5.7.3でイベントリスナー自体が削除されるため不要に

### 変更前
```javascript
NetworkState.rpcClient.addEventListener('disconnect', (event) => {
    // v5.7.1: Only process disconnect events while monitoring is active
    if (!NetworkState.ws.monitorActive) {
        return; // Ignore disconnect events after monitoring stopped
    }
    
    log('[WebSocket] 切断イベントを検出', 'warning');
    // ...
});
```

### 変更後
```javascript
NetworkState.rpcClient.addEventListener('disconnect', (event) => {
    // v5.7.4: Removed v5.7.1 workaround - no longer needed since v5.7.3 
    // removes all event listeners in stopMonitoring
    
    log('[WebSocket] 切断イベントを検出', 'warning');
    // ...
});
```

## 理由
- v5.7.3で`removeAllEventListeners()`を実装したことにより、stopMonitoring時にイベントリスナー自体が削除される
- そのため、monitorActiveチェックによる回避策は不要になった
- コードの可読性向上と技術的負債の削減

## 影響
- **機能への影響**: なし（機能的な変更はない）
- **パフォーマンス**: わずかに向上（不要な条件チェックを削除）
- **保守性**: 向上（コードがシンプルになった）

## テスト項目
1. WebSocket切断イベントが正常に検出されること
2. 監視停止後に切断イベントログが出ないこと（v5.7.3の機能で保証）
3. 複数回の監視開始・停止が正常に動作すること

## 変更規模
- **最小限**: 4行削除、2行のコメント追加
- **影響範囲**: disconnectイベントハンドラーのみ
- **リスク**: なし（機能的な変更はない）
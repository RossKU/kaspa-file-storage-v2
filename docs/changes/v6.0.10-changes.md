# v6.0.10: UI English Translation - Phase 7 Complete

## Changes
- Translated 25+ prompts and confirmation messages from Japanese to English
- Version update: v6.0.9 → v6.0.10
- Phase 7 of UI internationalization completed

## File Entry Prompts (10 items)
- TxID:BlockID:Password形式で入力してください → "Enter in TxID:BlockID:Password format"
- ファイルエントリーのTxID:BlockID:Password形式で入力してください → "Enter file entry in TxID:BlockID:Password format"
- File Nameを入力してください → "Enter file name"
- パス (省略可能、例: folder1/folder2) → "Path (optional, e.g.: folder1/folder2)"
- ディレクトリ名を入力してください → "Enter directory name"
- 親ディレクトリパス (省略可能、例: parent/folder) → "Parent directory path (optional, e.g.: parent/folder)"
- 既存のディレクトリメタデータを参照しますか？ → "Do you want to reference existing directory metadata?"
- ディレクトリのTxID:BlockID形式で入力してください → "Enter directory TxID:BlockID format"
- ディレクトリ「X」の中身も展開しますか？ → "Do you want to expand the contents of directory \"X\"?"
- 新規フォルダ名を入力してください → "Enter new folder name"

## Alert Messages (8 items)
- ログをクリップボードにコピーしました → "Log copied to clipboard"
- ディレクトリ表示機能が利用できません → "Directory display function is not available"
- 未対応のKaspaファイル形式です → "Unsupported Kaspa file format"
- .kaspaファイルをドロップしてください → "Please drop a .kaspa file"
- 作業フォルダが設定されていません → "Workspace is not set"
- パスワードを入力してください（8文字以上） → "Enter password (8 characters or more)"
- X個のファイルをリストから削除しますか？ → "Remove X file(s) from the list?"
- 情報表示は1つのファイルのみ選択してください → "Please select only one file for information display"

## Upload Estimate (7 items)
- アップロード見積もり → "Upload Estimate"
- ファイルサイズ → "File Size"
- ペイロード数: Xチャンク → "Payloads: X chunks"
- メタトランザクション → "Meta Transactions"
- スーパーメタトランザクション → "Super Meta Transactions"
- 合計トランザクション数 → "Total Transactions"
- 推定コスト → "Estimated Cost"
- 推定時間: X秒 (Y分) → "Estimated Time: X seconds (Y minutes)"

## Additional UI (3 items)
- エラー: → "Error:"
- 登録 → "Register"
- 同じファイルを選択してアップロードを続行してください → "Please select the same file to continue uploading"
- レジューム: X/Y チャンク → "Resume: X/Y chunks"
- レジューム可能な進捗を検出 → "Resumable progress detected"
- レジューム準備完了 → "Resume ready"

## Summary
Total: 25+ prompts and confirmations translated
Phase 7 of v6.0.x UI internationalization successfully completed
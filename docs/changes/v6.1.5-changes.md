# v6.1.5 Changes - REST API フォールバック修正

## 🔧 Fixed Issues

### REST APIフォールバックが動作しない問題を修正
- **問題**: RPCで "cannot find header" エラー時にREST APIフォールバックが動作しない
- **原因**: BlockIDが見つからない時にエラーをthrowしていた
- **修正内容**:
  1. 9779行目: Block not found時にthrowせず、警告ログを出してREST APIに移行
  2. RPCエラー全体をtry-catchで囲み、エラー時はREST APIフォールバックに移行

## 📝 Technical Details

### 修正前の動作
```
1. RPCでgetBlock()実行
2. Block not found → throw Error
3. エラーがcatchされ再throw
4. REST APIフォールバックに到達しない
```

### 修正後の動作
```
1. RPCでgetBlock()実行
2. Block not found → 警告ログ出力
3. REST APIフォールバックに移行
4. Explorer API経由でデータ取得
```

## 🎯 Benefits
- 古いブロックや未確認ブロックへのアクセスが改善
- 非アーカイブノードでもREST API経由でデータ取得可能
- エラーハンドリングの改善

## 📅 Release Date
- 2025-08-17
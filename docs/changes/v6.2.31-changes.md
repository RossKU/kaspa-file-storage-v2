# v6.2.31 Changes - Correct Fix for .kaspa File CID (Store Full, Display Short)

## Date: 2025-08-20

## Problem
v6.2.30's approach was incorrect. The proper solution is to:
- Store the full 43-character CID in KENV entries (preserve all information)
- Use only the first 8 characters when generating .kaspa filenames

## Solution
1. **Reverted line 13781** - Store full CID in KENV:
```javascript
// Correct: Store full CID for data integrity
kenvEntry.cid = window.currentFileCIDFull || window.currentFileCID || '';
```

2. **Fixed .kaspa file generation** (lines 5829-5832):
```javascript
// Extract first 8 characters for filename
const cidFull = kenvEntry.cid || 'unknown';
const cid = cidFull.length > 8 ? cidFull.slice(0, 8) : cidFull;
const filename = `${kenvEntry.name}.${cid}.kaspa`;
```

3. **Fixed directory .kaspa generation** (lines 5736-5738):
```javascript
// Same logic for directory files
const cidFull = kenvEntry.cid || 'dir';
const cidShort = cidFull.length > 8 ? cidFull.slice(0, 8) : cidFull;
const filename = `${kenvEntry.name}.${cidShort}.kaspa`;
```

## Impact
- Full CID (43 chars) is preserved in KENV entries for data integrity
- .kaspa filenames use short CID (8 chars) for usability
- Works for both new uploads and existing entries with full CIDs
- No data loss, better user experience

🤖 Generated with [Claude Code](https://claude.ai/code)

Co-Authored-By: Claude <noreply@anthropic.com>
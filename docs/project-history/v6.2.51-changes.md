# v6.2.51: Fix duplicate buttons in Download tab directory tree

## Problem
- Multiple buttons (🗑️ 🔧 📁 🔧) appearing in file entries instead of correct buttons (📋 📥)
- Buttons were being added multiple times or from wrong sources

## Solution
1. **Clear pendingTreeButtons array before rendering** (line 15005)
   - Ensures no leftover buttons from previous renders

2. **Check for existing buttons before adding** (line 15030)
   - Added `placeholder.children.length === 0` check
   - Prevents duplicate button creation

3. **Prevent duplicate button IDs** (line 6014)
   - Check if button ID already exists in array
   - Avoids adding same button multiple times

4. **English translation of button titles** (lines 15035, 15046)
   - "Copy TxID:BlockID" instead of Japanese text
   - "Download" instead of Japanese text

## Result
- Each file now shows only 2 buttons: 📋 (Copy) and 📥 (Download)
- No more duplicate or incorrect buttons
- Clean tree view in Download tab
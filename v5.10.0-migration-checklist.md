# v5.10.0 chunkBoundaries → segmentBoundaries 移行チェックリスト

## 変更概要
- プロパティ名: `chunkBoundaries` → `segmentBoundaries`
- バージョン: v5.9.4 → v5.10.0
- 変更理由: 1MBセグメントと22KBチャンクの概念を明確に区別

## 変更パターン別対応

### 1. 変数宣言（3箇所）
- [ ] 行4494: `const chunkBoundaries = [];` → `const segmentBoundaries = [];`
- [ ] 行5684: `const chunkBoundaries = [];` → `const segmentBoundaries = [];`
- [ ] 行8049: `const chunkBoundaries = [];` → `const segmentBoundaries = [];`

### 2. プロパティ代入（7箇所）
- [ ] 行3293: `chunkBoundaries: item.metadata?.metadata?.chunkBoundaries || undefined`
- [ ] 行4540: `chunkBoundaries: chunkBoundaries,` → `segmentBoundaries: segmentBoundaries,`
- [ ] 行5745: `chunkBoundaries: chunkBoundaries,` → `segmentBoundaries: segmentBoundaries,`
- [ ] 行8107: `FileState.progressManager.progress.metadata.chunkBoundaries = chunkBoundaries;`
- [ ] 行8777: `FileState.progressManager.progress.metadata.chunkBoundaries = chunkBoundaries;`
- [ ] 行9210: `chunkBoundaries: useNewApproach ? ... : undefined`
- [ ] 行11142: `kaspaMetadata.metadata.chunkBoundaries = matchingEntry...`

### 3. 配列操作（4箇所）
- [ ] 行5688: `chunkBoundaries.push({` → `segmentBoundaries.push({`
- [ ] 行8070: `chunkBoundaries.push({` → `segmentBoundaries.push({`
- [ ] 行9838: `fileMetadata.metadata.chunkBoundaries.filter(boundary => {`
- [ ] 行11088: `allChunkBoundaries.push(...intMeta.metadata.chunkBoundaries);`

### 4. プロパティアクセス（読み取り）（19箇所）
- [ ] 行6216: `metadata.metadata?.chunkBoundaries`
- [ ] 行6220: `metadata.metadata.chunkBoundaries[index]`
- [ ] 行6221: `metadata.metadata.chunkBoundaries[index]`
- [ ] 行6342: `metadata.metadata?.chunkBoundaries`
- [ ] 行6345: `const boundaries = metadata.metadata.chunkBoundaries;`
- [ ] 行6431: `metadata.metadata?.chunkBoundaries`
- [ ] 行6433: `metadata.metadata.chunkBoundaries.length`
- [ ] 行6472: `const boundaries = metadata.metadata.chunkBoundaries;`
- [ ] 行6542: `metadata.metadata?.chunkBoundaries?.[i]`
- [ ] 行9506: `FileState.kaspaMetadata.metadata?.chunkBoundaries`
- [ ] 行9507: `const boundaries = FileState.kaspaMetadata.metadata.chunkBoundaries;`
- [ ] 行9829: `fileMetadata.metadata?.chunkBoundaries`
- [ ] 行11059: `intMetaData.metadata?.chunkBoundaries?.length`
- [ ] 行11060: `intMetaData.metadata?.chunkBoundaries?.length`
- [ ] 行11061: `intMetaData.metadata.chunkBoundaries[0]`
- [ ] 行11087: `intMeta.metadata?.chunkBoundaries`
- [ ] 行11098: `intMeta.metadata?.chunkBoundaries?.length`
- [ ] 行11141: `matchingEntry.metadata?.metadata?.chunkBoundaries`
- [ ] 行11143: `FileState.kaspaMetadata.metadata.chunkBoundaries.length`

### 5. 戻り値・引数（2箇所）
- [ ] 行8114: `return { payloads, key, salt, chunkBoundaries };`
- [ ] 行9851: `return { chunkBoundaries: boundariesForGroup };`

### 6. ログメッセージ（6箇所）
- [ ] 行3291: コメント「chunkBoundariesとpayloadSplitフラグを追加」
- [ ] 行9505: コメント「chunkBoundariesの全体状況をログに出力」
- [ ] 行9508: ログ「全chunkBoundaries数:」
- [ ] 行9509: ログ「chunkBoundariesインデックス:」
- [ ] 行11059: ログ「chunkBoundaries:」
- [ ] 行11116: 条件分岐 `{ chunkBoundaries: allChunkBoundaries }`

## 変更手順

1. **バックアップ作成**
   - v5.9.4.htmlのコピーを作成

2. **新ファイル作成**
   - v5.10.0.htmlとして新規作成

3. **段階的変更**
   - 変数宣言から開始
   - プロパティ代入
   - プロパティアクセス
   - ログメッセージ

4. **動作確認ポイント**
   - [ ] ファイルアップロード（payloadSplitモード）
   - [ ] ファイルダウンロード
   - [ ] スーパーメタデータ作成
   - [ ] 中間メタデータ処理
   - [ ] デバッグログ出力

5. **特に注意すべき箇所**
   - 行6220-6221: 直接配列アクセス（エラーハンドリング要確認）
   - 行9838: filterメソッド（オプショナルチェーン要確認）
   - 行11088: スプレッド演算子（既にifでガード済み）

## 置換時の注意事項

1. **単純置換できない箇所**
   - allChunkBoundaries変数（ローカル変数のため変更不要）
   - boundariesForGroup変数（ローカル変数のため変更不要）

2. **コメント・ログの更新**
   - 「チャンクバウンダリ」→「セグメントバウンダリ」
   - 英語表記も統一

3. **デバッグログの追加**
   - 移行確認用のログを一時的に追加
   - v5.10.0でのみ出力される識別ログ
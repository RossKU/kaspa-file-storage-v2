# v5.10.11 変更内容

## 🛠️ 概要
ディレクトリビルダー機能を実装しました。カプセル化されたアーキテクチャで、グローバル変数を使用せずに実装。

## 🔧 変更内容

### 新機能: ディレクトリビルダー
- **DirectoryBuilderState**: 新しいステートオブジェクトを追加
- **3段階のワークフロー**:
  1. インポート段階（新規作成、TxIDから読込、.kaspaから読込）
  2. 編集段階（エントリー追加/削除、プレビュー）
  3. エクスポート段階（.kaspa生成、ブロックチェーンアップロード、Fileタブ登録）

### アーキテクチャ改善
- グローバル関数を廃止し、DirectoryBuilderStateにカプセル化
- 既存のStateパターン（UserState、FileState等）に準拠
- onclick属性からDirectoryBuilderStateのメソッドを直接呼び出し

### UI/UX
- 新しい「Dir Builder」タブをサイドバーに追加
- エクスプローラー風のエントリー管理UI
- リアルタイムメタデータプレビュー

## 🚀 使い方
1. サイドバーの「Dir Builder」タブをクリック
2. 「新規作成」ボタンでゼロから開始、または既存データを読み込み
3. ファイル/ディレクトリエントリーを追加
4. 「保存」後、エクスポート方法を選択

## 📝 技術詳細
- v3.4.1仕様に準拠したメタデータ生成
- type: 'directory'エントリーのサポート
- CIDフィールドのnull許可に対応

## 🔨 今後の実装予定
- ブロックチェーンアップロード機能の完成
- ドラッグ&ドロップによるエントリー並び替え
- TxIDからファイルメタデータを自動取得
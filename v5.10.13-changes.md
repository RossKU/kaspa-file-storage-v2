# v5.10.13 変更内容

## 🔨 概要
ディレクトリビルダーの基本機能を実装し、UIを大幅に改善しました。

## 🔧 変更内容

### エクスポート機能の強化
- **パスワード入力フィールド**: 8文字以上の強力なパスワードで保護
- **生成のみ**: メタデータを生成して.kaspaファイルとして保存
- **生成してアップロード**: ブロックチェーンに直接アップロード
- 既存の`createDirectoryMetadata`と`uploadDirectoryMetadata`関数を再利用

### UI/UXの改善
- **エントリー一覧を削除**: 重複表示を解消
- **ビジュアルプレビューに削除ボタン**: 各エントリーカードに✕ボタンを追加
- **エントリー追加の改良**:
  - ファイル: 名前とパスを個別に入力可能
  - ディレクトリ: 親パスの指定、既存メタデータの参照オプション

### Pathの設計方針
- **ファイルエントリー**: `path`はファイルが存在するディレクトリパス
- **ディレクトリエントリー**: 実際のメタデータを持つディレクトリのみ追加
- 単なるパス表現のためのディレクトリエントリーは不要

## 📋 実装詳細

### DirectoryBuilderStateの新メソッド
- `generateOnly()`: メタデータ生成のみ
- `generateAndUpload()`: 生成とブロックチェーンアップロード
- パスワード検証（8文字以上）
- 一時的なメタデータ保存（window.tempDirectoryMetadata）

### エントリー追加の改善
```javascript
// ファイルエントリー
- TxID:BlockID:Password
- ファイル名入力
- パス入力（省略可）

// ディレクトリエントリー
- ディレクトリ名入力
- 親パス入力（省略可）
- 既存メタデータ参照オプション
```

## 🎯 今後の実装予定
- TxIDから実際のファイルメタデータを取得
- パス構造のツリービュー表示
- ドラッグ&ドロップでのエントリー並び替え
# v5.10.21 変更内容

## 🎯 概要
アーカイブノード自動検出・管理機能を統合し、30時間以上前のデータへのアクセス信頼性を大幅に改善しました。

## 🔧 変更内容

### 1. ArchiveNodeManagerクラスの実装
- 動的なアーカイブノード判定基準（30時間分のブロック保持）
- 最大5つのアーカイブノードとの接続を自動維持
- 応答時間ベースの負荷分散アルゴリズム
- ノードの健全性監視と自動フェイルオーバー

### 2. NetworkStateへの統合
```javascript
NetworkState.archiveManager = new ArchiveNodeManager();
```

### 3. トランザクション取得の拡張
- `getTransactionWithArchive()`関数を追加
- 通常ノードで「pruned」エラーが発生時、自動的にアーカイブノードへフォールバック
- 応答時間の記録と最適なノード選択

### 4. UI機能の追加

#### Statusタブ
- アーカイブノード接続状態の表示
- 接続数と健全性の可視化
- 平均応答時間の表示

#### Debugタブ
- アーカイブノードモニターセクションを追加
- 個別ノードの詳細情報表示
- 応答時間テスト機能
- 探索開始/停止コントロール

## 📊 技術仕様

### アーカイブノード判定基準
```javascript
// 基準時間からの必要ブロック数を動的計算
baselineHours = 30 // デフォルト30時間
blocksPerSecond = 10 // Kaspaのブロック生成速度
requiredBlocks = baselineHours * 3600 * blocksPerSecond
```

### 負荷分散アルゴリズム
- 直近10回の応答時間を記録
- 平均応答時間でソート
- 70%を最速ノード、30%を2番目に割り振り

## 🚀 効果

1. **信頼性向上**
   - 30時間以上前のデータ取得成功率が大幅改善
   - 5つのアーカイブノードによる冗長性確保

2. **パフォーマンス**
   - 応答時間ベースの最適化
   - 不健全なノードの自動除外

3. **透過的な統合**
   - 既存のコードへの変更最小限
   - 自動フォールバック機能

## 📋 互換性

- 既存のダウンロード機能との完全互換性
- アーカイブノードが利用できない場合は従来動作にフォールバック

## 🔍 今後の改善案

1. アーカイブノードの地理的分散考慮
2. より詳細な統計情報の収集
3. カスタム基準時間の設定UI
4. アーカイブノードプールの永続化
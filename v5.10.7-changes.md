# v5.10.7 変更内容

## 🔧 修正内容

### v3.4.0ディレクトリメタデータの読み込みサポート

**問題**：
- v5.10.6はディレクトリメタデータをv3.4形式で作成
- しかし、読み込み時のバージョンチェックにv3.4が含まれていなかった
- 結果、v5.10.6で作成したディレクトリを読み込めない状態

**エラーメッセージ**：
```
復号化エラー: これは有効なメタトランザクションではありません
```

**修正内容**：
```javascript
// 変更前
if ((metaData.version === "2.1" || metaData.version === "3.3" || metaData.version === "3.1.1") && 
    (metaData.type === "kaspa-meta-tx" || metaData.type === "kaspa-directory")) {

// 変更後
if ((metaData.version === "2.1" || metaData.version === "3.3" || metaData.version === "3.1.1" || metaData.version === "3.4") && 
    (metaData.type === "kaspa-meta-tx" || metaData.type === "kaspa-directory")) {
```

## 📝 リリースノート

**日付**: 2025年7月24日
**バージョン**: v5.10.7
**ステータス**: バグ修正

このバージョンはv5.10.6の実装漏れを修正し、v3.4.0形式のディレクトリメタデータを正しく読み込めるようにしました。

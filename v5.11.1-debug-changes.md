# v5.11.1 Debug Changes

## 問題の原因
- estimateBlockCount実装により、多くのノードがpruned nodeと判定されていた
- `!serverInfo.hasUtxoIndex`の条件により、実際には7.4Mブロックを持つノードでも推定値が2.592M（3日分）に制限されていた
- これにより、5Mの閾値を満たさないと判定されていた

## 変更内容

### 1. 推定値の使用を停止
- `estimateBlockCount`の使用を一時的に停止
- 実際のブロック数（`dagInfo.blockCount`）をそのまま使用
- 閾値判定も実際のブロック数で実施

### 2. ログ出力の削減
- Archive Node Managerの初期化・停止ログを削除
- ネットワーク切り替え、閾値更新のログを削除
- 通常の動作ログを最小限に

### 3. getBlock詳細ログの追加
- `testBlockRetrieval`でブロック取得の失敗時のみエラーログを出力
- 成功時はログを出さない（ログの混雑を防ぐ）
- エラー時は具体的な高さとエラーメッセージを表示

## 結果
- 7.4Mブロックのノードが正しくアーカイブノードとして判定されるはず
- ログの混雑が解消される
- getBlockの失敗原因が明確になる

## 今後の課題
- pruned nodeの判定ロジックの改善が必要
- `hasUtxoIndex`以外の方法でアーカイブノードを判定する必要がある
- 実際のブロック取得可能範囲を調査する方法の検討
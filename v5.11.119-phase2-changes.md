# v5.11.119 Phase 2: アーカイブノード自動探査と強制使用モード（Ultrathink）

## 概要
アーカイブノード探査を自動化し、ユーザーが混乱しないようにトグルスイッチの有効/無効を動的に制御。探査UIは既存のものをそのまま使用。

## 実装内容

### 1. アーカイブノード探査の自動開始（2746-2747行）
```javascript
// v5.11.119 Phase 2: Auto-start discovery on initialize
this.startDiscovery();
```
- 初期化時に自動的にバックグラウンドで探査開始
- ユーザーが手動で開始する必要なし

### 2. 100連続失敗で探査停止（2732-2734行、2848-2854行）
```javascript
// Track consecutive non-archive nodes
this.consecutiveNonArchive = 0;
this.maxConsecutiveNonArchive = 100;

// Stop after 100 consecutive failures
if (this.consecutiveNonArchive >= this.maxConsecutiveNonArchive) {
    this.isDiscovering = false;
    this.maintainConnections = false;
    this.addEvent(`Stopped after ${this.maxConsecutiveNonArchive} non-archive nodes`, '🛑');
}
```

### 3. トグルスイッチの動的制御（10125-10166行）
```javascript
// Update force archive node toggle state
if (nodeCount === 0) {
    toggle.disabled = true;
    toggle.checked = false; // Reset when disabled
    
    if (manager.isDiscovering) {
        label.innerHTML = `
            アーカイブノード強制使用<br>
            <small style="color: var(--warning-color);">
                アーカイブノードを探索中... (${manager.stats.totalAttempts}回試行)
            </small>
        `;
    } else if (manager.consecutiveNonArchive >= manager.maxConsecutiveNonArchive) {
        label.innerHTML = `
            アーカイブノード強制使用<br>
            <small style="color: var(--error-color);">
                アーカイブノードが見つかりません (${manager.maxConsecutiveNonArchive}回連続失敗)
            </small>
        `;
    }
} else {
    toggle.disabled = false;
    label.innerHTML = `
        アーカイブノード強制使用<br>
        <small style="color: var(--success-color);">
            ${nodeCount}個のアーカイブノードが利用可能
        </small>
    `;
}
```

### 4. 強制使用モードの実装（9254行、9365-9373行、9444-9452行）
```javascript
// DownloadManagerに追加
this.forceArchiveNode = false; // v5.11.119 Phase 2: Force use of archive nodes

// ダウンロード時のロジック
if (this.forceArchiveNode && NetworkState.archiveManager && NetworkState.archiveManager.connections.size > 0) {
    const archiveClient = await NetworkState.archiveManager.getOptimalClient();
    if (archiveClient) {
        rpcClient = archiveClient;
        log(`[DownloadManager] アーカイブノード強制使用モード - アーカイブノードを使用`, 'debug');
    }
}
```

### 5. UIの追加（1900-1909行）
```html
<div class="setting-row">
    <div class="setting-label" id="forceArchiveNodeLabel">
        アーカイブノード強制使用<br>
        <small style="color: var(--text-secondary);">アーカイブノードを探索中...</small>
    </div>
    <label class="toggle-switch">
        <input type="checkbox" id="forceArchiveNodeToggle" disabled onchange="toggleForceArchiveNode()">
        <span class="toggle-slider"></span>
    </label>
</div>
```

## 動作フロー

1. **アプリ起動時**
   - アーカイブノード探査が自動開始
   - トグルは無効状態（グレーアウト）
   - 「探索中...」メッセージ表示

2. **アーカイブノード発見時**
   - トグルが有効化
   - 「N個利用可能」メッセージ表示（緑色）
   - ユーザーが強制使用を選択可能

3. **100回連続失敗時**
   - 探査自動停止
   - 「100回連続失敗」メッセージ表示（赤色）
   - トグルは無効のまま

## メリット

1. **ユーザビリティ向上**
   - 手動探査不要
   - 状態が一目でわかる
   - 混乱を防ぐ

2. **リソース効率**
   - 100回で自動停止
   - 無駄な探査を防ぐ

3. **既存UIの活用**
   - アーカイブノードモニターはそのまま使用
   - 詳細な状態確認可能
# v5.11.122: 日数計算修正とアーカイブノード表示追加

## 変更内容

### 1. 日数計算をシンプルに修正
- ハードフォークを考慮した複雑な計算を削除
- 10BPSベースのシンプルな計算に戻す
- 計算式: `days = blueScore / 864,000`

### 2. アーカイブノード詳細に日数表示を追加
- ステータスタブのアーカイブノードモニターに日数を表示
- 表示例: `N1: Full/217M (251.8days+)/45ms ✅`

## 実装詳細

### calculateDaysFromBlocks関数（7150-7156行）
```javascript
function calculateDaysFromBlocks(blueScore) {
    // Simple calculation based on 10 BPS
    const blocksPerDay = 864000; // 86400 seconds * 10 blocks/sec
    const days = blueScore / blocksPerDay;
    return days.toFixed(1);
}
```

### アーカイブノード表示改善（10132-10139行）
```javascript
// v5.11.122: Add days display
let daysInfo = '';
if (node.blockCount) {
    const days = calculateDaysFromBlocks(node.blockCount);
    daysInfo = ` (${days}days+)`;
}
```

## 修正理由
- 複雑な計算は正確性に欠けるため
- シンプルな10BPSベースで十分（「+」により概算であることを示す）
- アーカイブノードの表示が欠けていた問題を修正
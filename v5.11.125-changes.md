# v5.11.125: アーカイブノード初回応答時間測定

## 変更内容

### 1. 初回接続時の応答時間測定を実装
- **問題**: アーカイブノードの応答時間が常に0msと表示されていた
- **原因**: `getBlockDagInfo`実行時の応答時間を測定していなかった
- **修正**: 初回クエリの応答時間を測定し、responseTimes配列に格納

### 2. 実装詳細（2809-2813行）
```javascript
// v5.11.125: Measure initial response time
const startTime = Date.now();
const dagInfo = await client.getBlockDagInfo();
const initialResponseTime = Date.now() - startTime;
```

### 3. 初期応答時間の記録（2841行）
```javascript
responseTimes: [initialResponseTime], // v5.11.125: Include initial response time
```

### 4. イベントログへの表示（2827行）
```javascript
this.addEvent(`Initial response: ${initialResponseTime}ms`, '⏱️'); // v5.11.125
```

## 効果

- アーカイブノード接続直後から正確な応答時間が表示される
- ステータスタブで各ノードの実際の応答時間が確認可能
- ノード選択アルゴリズムがより正確に動作
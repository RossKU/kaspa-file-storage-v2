# v5.11.128: DirectoryBuilder依存関数の復元

## 問題
v5.11.127でディレクトリアップロード機能を削除した際、DirectoryBuilderが使用する`uploadDirectoryMetadata`関数内で呼び出されている以下の関数も削除してしまった：
- createTransaction
- sendTransaction  
- waitForConfirmation

これによりDirectoryBuilderでディレクトリメタデータをアップロードしようとすると`createTransaction is not defined`エラーが発生。

## 修正内容
DirectoryBuilderの動作を維持するため、以下の関数を復元：

1. **createTransaction** - トランザクション作成
2. **sendTransaction** - トランザクション送信（エラーハンドリング付き）
3. **waitForConfirmation** - BlockID取得待機（WebSocket/RPC/Fallback）

注：fetchBlockIdFromExplorerは使用されないため復元せず。

## 影響範囲
- DirectoryBuilderのディレクトリメタデータアップロード機能が正常に動作するように
- 通常のファイルアップロードには影響なし（別の仕組みを使用）
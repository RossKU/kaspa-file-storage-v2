# v5.11.3 Changes - 閾値判定＋手動テスト方式

## 主な変更内容

### 1. アーカイブノード判定の簡略化
- 複雑な段階的ブロック取得テストを削除
- シンプルな閾値チェックのみで判定
- `getBlockByHeight` APIが存在しない問題を回避

### 2. 手動テスト機能の実装
- **テスト実行**ボタンを追加
- ユーザーが任意のブロックハッシュを入力してテスト可能
- ハッシュ未入力時は最新のtipブロックでテスト

### 3. UIの改善
```html
<!-- 追加されたUI要素 -->
<button onclick="...testBlocks()">テスト実行</button>
<input type="text" id="testBlockHash" placeholder="テスト用ブロックハッシュ (任意)">
```

### 4. testBlocks()メソッドの実装
```javascript
async testBlocks() {
    // ユーザー指定のハッシュまたはtipハッシュでテスト
    // 各接続ノードに対してgetBlockを実行
    // 成功/失敗をログとイベントに記録
}
```

## 利点

1. **シンプルで確実な動作**
   - 存在しないAPIに依存しない
   - エラーが発生しにくい

2. **ユーザーによる検証**
   - 古いブロックハッシュでテストすることで本当のアーカイブノードか確認可能
   - 透明性の向上

3. **デバッグの容易さ**
   - 問題の切り分けが簡単
   - ログが見やすい

## 使用方法

1. アーカイブノードモニターで「探索」をクリック
2. 閾値を超えるノードが自動的にアーカイブと判定される
3. 実際に古いデータを持っているか確認したい場合：
   - 古いブロックのハッシュを入力
   - 「テスト実行」をクリック
   - 結果を確認

## 技術的詳細

- `validateArchiveNode`: 閾値チェックのみ実施
- `testBlockRetrieval`: 削除（不要になったため）
- `testBlocks`: 新規実装（v29の同名関数を参考に）
# v5.11.41 変更内容 - KENV Phase 1-B修正 プロパティ名統一

## 実装日時
2025-07-30

## 概要
v5.11.40で発生したuploadDataプロパティ名の不一致によるエラーを修正。ファイルアップロード時の正しいプロパティ名を使用し、防御的プログラミングを追加。

## 問題分析

### 1. uploadDataプロパティ構造の違い

**ファイルアップロード時**:
```javascript
{
    fileName: currentFile.name,    // v5.11.40は"name"を期待
    fileSize: currentFile.size,    // v5.11.40は"size"を期待  
    fileSha256: fileHash,          // v5.11.40は"sha256"を期待
    totalCost: totalCost,          // v5.11.40は"uploadCost"を期待
}
```

**ディレクトリアップロード時**:
```javascript
{
    name: dirName,                 // 存在する
    totalSize: ...,                // fileSize相当
    totalCost: totalCost,          // 存在する
}
```

### 2. エラーの原因
- uploadData.name（undefined）→ addMinimalEntry
- entry.name（undefined）→ updateIndex
- entry.name.substring() → **Cannot read properties of undefined**

## 主な修正内容

### 1. KENVエントリー追加時のプロパティ名修正（13003-13016行）
```javascript
// 修正前（v5.11.40）
uploadData.name        → uploadData.fileName
uploadData.size        → uploadData.fileSize
uploadData.sha256      → uploadData.fileSha256
uploadData.uploadCost  → uploadData.totalCost
uploadData.uploadedBy  → UserState.address?.toString() || null
uploadData.encrypted   → true（ファイルは常に暗号化）
```

### 2. updateIndex関数の防御的プログラミング（3363-3389行）
```javascript
// エントリーまたは名前の有効性チェック
if (!entry || !entry.name) {
    log('[KENV] updateIndex: エントリーまたは名前が無効です', 'error');
    return;
}

// 日付の有効性チェック
if (entry.uploadDate) {
    const timestamp = new Date(entry.uploadDate).getTime();
    if (!isNaN(timestamp)) {
        // インデックス更新
    }
}
```

## 動作検証項目

### 1. ファイルアップロード時
- [ ] ファイル選択後、アップロード開始
- [ ] エラーログが出ないこと
- [ ] KENVエントリーが正常に追加されること
- [ ] File Tabにファイルが表示されること
- [ ] workspace.kentryファイルにエントリーが保存されること

### 2. メタTx作成時
- [ ] メタTx作成が正常に完了すること
- [ ] KENVエントリーのmetaTxId/metaTxBlockIdが更新されること
- [ ] statusが'verified'に変更されること

### 3. File Tab表示時
- [ ] エラーなくファイル一覧が表示されること
- [ ] ソート・フィルタが正常に動作すること
- [ ] 検索機能が正常に動作すること

### 4. エラーハンドリング
- [ ] 不正なデータでもクラッシュしないこと
- [ ] 適切なエラーログが出力されること

## 今後の改善点

### Phase 2: データ構造の統一（v5.11.42予定）
- uploadData構造をファイル/ディレクトリで統一
- 共通インターフェースの作成
- プロパティ名の一貫性確保

### Phase 3: 堅牢性向上（v5.11.43予定）
- より包括的なundefined/nullチェック
- エラー時の自動復旧処理
- データ整合性の検証強化

## テスト推奨事項
1. 小さなファイル（< 1MB）でテスト
2. エラーログの確認
3. workspace.kentryファイルの内容確認
4. File Tab表示の動作確認
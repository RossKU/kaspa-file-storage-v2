# v5.11.42 変更内容 - リファクタリング関数名修正

## 実装日時
2025-07-30

## 概要
v5.11.41でKENVのバグは修正したが、リファクタリング時の関数名変更による別のエラーを修正。

## 発見された問題

### 1. formatFileSize関数未定義エラー
- **場所**: File Tab表示時（16907行）
- **原因**: formatFileSize → formatSize に関数名が変更されていた
- **影響**: File Tabでファイル一覧が表示できない

### 2. WorkspaceHandlers未定義エラー
- **場所**: HTML内のonclick/onchange属性
- **原因**: WorkspaceHandlersオブジェクトがグローバルスコープに公開されていない
- **影響**: 進捗ファイル読み込み機能が使用できない

## 修正内容

### 1. 関数名の統一（16907行）
```javascript
// 修正前
const size = entry.fileSize ? formatFileSize(entry.fileSize) : '-';

// 修正後
const size = entry.fileSize ? formatSize(entry.fileSize) : '-';
```

### 2. HTML属性の修正（1517-1520, 1527行）
```html
<!-- 修正前 -->
<button onclick="WorkspaceHandlers.loadProgressFile()">
<input onchange="WorkspaceHandlers.handleProgressFileSelect(event)">
<button onclick="WorkspaceHandlers.cancelResume()">

<!-- 修正後 -->
<button onclick="loadProgressFile()">
<input onchange="handleProgressFileSelect(event)">  
<button onclick="cancelResume()">
```

### 3. JavaScript内の呼び出し修正（16427行）
```javascript
// 修正前
const resumeResult = await WorkspaceHandlers.checkAutoResumeProgress(file);

// 修正後  
const resumeResult = await checkAutoResumeProgress(file);
```

## 技術的背景

### リファクタリングの影響
- Phase 5（v5.5.0）のリファクタリングで関数がオブジェクトにカプセル化された
- 後方互換性のため個別関数は公開されているが、オブジェクト自体は未公開だった
- windowオブジェクトを使わない方針に従い、既存の個別関数を使用するよう修正

## 動作確認項目

- [x] formatSize関数が正しく呼ばれること
- [x] File Tabでファイル一覧が表示されること
- [x] 進捗ファイル読み込みボタンが動作すること
- [x] レジュームキャンセルボタンが動作すること
- [x] 自動レジューム機能が動作すること

## 今後の注意点
- リファクタリング時は使用箇所をすべて確認
- 関数名変更時は全置換を確実に実施
- HTMLのイベント属性も含めて検索対象とする
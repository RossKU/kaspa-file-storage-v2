# v5.11.6 Changes - Archive Logs to Window Display Only

## Changes
1. **Moved all archive logs from system debug log to archive window**
   - Replaced all `this.log()` calls with `this.addEvent()` to display in archive window
   - Removed system debug log outputs for archive manager operations
   
2. **Enhanced archive window event logging**
   - Connection info: "Connected to version"
   - BlockDAG info: "BlockDAG info: XM blocks"
   - Threshold check results
   - Discovery progress messages
   - Test results
   
3. **Removed redundant logs**
   - Deleted duplicate log messages
   - Simplified test block logging
   - Removed technical warnings (MAX_SAFE_INTEGER)

## Files Modified
- index.html (v5.11.5 → v5.11.6)
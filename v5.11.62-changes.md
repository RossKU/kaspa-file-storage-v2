# v5.11.62 変更内容 - Fileタブ共有メニューにTxIDコピー機能実装

## 実装日時
2025-07-31

## 概要
Historyタブを削除したことで失われたTxIDコピー機能を、Fileタブの共有メニューに移植しました。

## 変更内容

### shareAction()関数の実装
以前はプレースホルダーだった共有機能を実装：

1. **txid-password**アクション
   - TxID:BlockID:Password形式でクリップボードにコピー
   - パスワードが保存されていない場合はエラー表示

2. **txid-no-password**アクション
   - TxID:BlockID形式でクリップボードにコピー
   - BlockIDがない場合はTxIDのみコピー

3. **password**アクション
   - パスワードのみをクリップボードにコピー

### データソース
- KENVエントリー（`FileState.kenvManager.entries`）から直接データを取得
- 履歴システムに依存しない実装

### エラーチェック
- ファイル未選択時の警告
- 複数ファイル選択時の警告
- metaTxIDが存在しない場合のエラー
- パスワードが保存されていない場合のエラー

## 影響
- ディレクトリビルダーで必要なMetaTxID:BlockID:Pass形式のデータをコピー可能に
- Historyタブなしでも同等の機能を提供

## テスト方法
1. Fileタブでファイルを1つ選択
2. Shareボタンをクリックしてメニューを表示
3. 「🔐 TxID (パスワード付き)」をクリック
4. クリップボードにTxID:BlockID:Password形式でコピーされることを確認
# v5.11.67 変更内容 - salt固定カラム追加とpayloadSplit修正

## 実装日時
2025-07-31

## 概要
KENVエントリーのCSVフォーマットにsaltを固定カラムとして追加し、payloadSplitフラグの判定ロジックを修正しました。これにより、.kaspaファイルからのダウンロードが正しく動作するようになります。

## 技術的変更内容

### 1. CSVヘッダーの変更（3563行目）
- 45カラムから46カラムに拡張
- authWarningの後にsaltカラムを追加

### 2. saveKentryFile関数（3616行目）
- 46番目のカラムとしてsaltを保存する処理を追加

### 3. loadKentryFile関数（3851行目、3854行目、3863行目）
- values[24]でpayloadSplitを明示的に読み込み
- values[46]でsaltを読み込み
- 可変カラムの開始位置を46から47に変更

### 4. generateKaspaFromKENV関数（17911行目）
- payloadSplitフラグの判定を修正
- 文字列'true'と真偽値trueの両方に対応

## 問題の修正

### 根本原因
1. **payloadSplitフラグの不整合**
   - KENVでは文字列'true'として保存
   - .kaspaファイル生成時に正しく判定されず、falseになっていた

2. **saltの欠落**
   - KENVフォーマットv3.1.3にsaltカラムが定義されていなかった
   - saltがないと復号化できない

### 修正結果
- saltが固定カラムとして正しく保存・読み込みされる
- payloadSplitフラグが正しく判定される
- .kaspaファイルからのダウンロードが成功する

## 影響と利点

1. **復号化の成功**: saltが正しく保存されるため、暗号化されたデータの復号化が可能
2. **PayloadSplit対応**: payloadSplitフラグが正しく処理され、新方式のファイルも正常にダウンロード可能
3. **後方互換性**: 既存のKENVファイルも問題なく読み込み可能（saltカラムが空でも動作）

## 検証方法
1. ファイルをアップロード（PayloadSplit方式）
2. Fileタブで.kaspaファイルを生成
3. 生成された.kaspaファイルでダウンロードが成功することを確認
# v5.11.8 Changes - Remove Global Variables

## Changes
1. **Removed all window.downloadManager references**
   - Changed all `window.downloadManager` to `NetworkState.downloadManager`
   - Fixed REST API test mode to properly use NetworkState
   - Maintains proper encapsulation following architecture map

2. **Fixed locations**
   - `toggleRestApiTest()`: Now uses `NetworkState.downloadManager`
   - `ParallelDownloader.workerRaw()`: Checks `NetworkState.downloadManager.forceRestApi`
   - `loadFromMetaTxId()`: Two locations updated for REST API test mode
   - `downloadFileFromDirectory()`: All references updated
   - `loadFromUnifiedInput()`: Uses NetworkState for metadata clearing
   - `downloadFile()`: executeDownload now uses NetworkState
   - Directory bulk download: Uses NetworkState consistently

## Purpose
- Follows proper architecture with NetworkState encapsulation
- Prepares for future removal of all window global variables
- Improves code maintainability and reduces global scope pollution

## Files Modified
- index.html (v5.11.7 → v5.11.8)
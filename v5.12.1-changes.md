# v5.12.1: ブロックイベントフィルター修正

## 問題
v5.12.0でフィルター処理を`MultiWebSocketTest.handleBlockAdded()`に実装してしまったが、これは別機能用で実際のアップロードでは使用されていなかった。

## 修正内容

### 1. 正しい場所にフィルター実装
**メインRPC**（15988行目）:
- `NetworkState.rpcClient.addEventListener('block-added')`の先頭にフィルター処理追加

**補助RPC**（15917行目）:
- `auxClient.addEventListener('block-added')`の先頭にフィルター処理追加

### 2. 実装詳細
```javascript
// v5.12.1: Block event filter
if (NetworkState.blockEventFilter.enabled) {
    const nodeId = // メインまたはaux-1〜4
    const shouldFilter = // フィルター条件チェック
    if (shouldFilter) {
        // カウンター更新
        // UI更新
        return; // イベント無視
    }
}
```

### 3. MultiWebSocketTestからフィルター削除
- 混乱を避けるため、誤った実装場所からフィルター処理を削除
- コメントで「v5.12.1で実際のRPCリスナーに移動」と記載

## 効果
- WebSocket接続を維持したままBlockIDイベントを正しくフィルタリング
- メインRPCと補助RPC両方で動作
- Mempoolには入るがブロックに含まれない状態を正確にシミュレート

## テスト手順
1. ブラウザリロード
2. システム初期化
3. デバッグタブ → ブロックイベントフィルター有効化
4. アップロード開始
5. ログで「🚫 [BlockFilter] BlockID受信したが無視」を確認
6. タイムアウトまで待機することを確認
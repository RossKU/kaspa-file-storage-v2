# v5.12.18: テーブルヘッダークリックソート

## 問題
- ソートの昇順/降順切り替えができない
- 名前は昇順のみ、サイズと日付は降順のみ固定
- ユーザーが自由にソート順を選択できない

## 解決案
テーブルヘッダークリック方式を実装（最も直感的でエレガント）

## 修正内容

### 1. テーブルヘッダーをクリック可能に（1658-1667行目）
```html
<th class="sortable" onclick="toggleSort('name')">
    ファイル名 <span id="sortArrowName" class="sort-arrow">↑</span>
</th>
<th class="sortable" onclick="toggleSort('size')">
    容量 <span id="sortArrowSize" class="sort-arrow"></span>
</th>
<th class="sortable" onclick="toggleSort('date')">
    UP日 <span id="sortArrowDate" class="sort-arrow"></span>
</th>
```

### 2. ソート状態管理を追加（17530-17540行目）
```javascript
window.sortState = {
    column: 'name',
    ascending: true
};
```

### 3. toggleSort関数を実装（17542-17565行目）
- クリックで昇順/降順トグル
- 矢印アイコンの更新（↑昇順、↓降順）
- アクティブな列の矢印を強調表示

### 4. sortKENVFiles関数を改良（17567-17610行目）
- 昇順/降順パラメータを追加
- すべてのソート項目で両方向対応
- サイズと日付のデフォルト降順を削除

### 5. CSSスタイルを追加（8-24行目）
- ソート可能な列にホバー効果
- 矢印アイコンのトランジション
- アクティブ状態の視覚フィードバック

## 効果
- **直感的な操作**: テーブルヘッダーをクリックするだけ
- **視覚的フィードバック**: 矢印で現在のソート状態を表示
- **完全な制御**: すべての列で昇順/降順切り替え可能
- **モダンなUX**: 多くのアプリケーションで採用されている標準的なUI

## 技術詳細
- `sortState`オブジェクトで状態管理
- 矢印の不透明度で非アクティブ状態を表現（opacity: 0.3）
- トランジション効果でスムーズな視覚変化
- セレクトボックスとの同期も維持（後方互換性）
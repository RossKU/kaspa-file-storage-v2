# v5.12.2: REST API Kill機能追加

## 概要
REST APIを無効化/Kill可能にする機能を実装。プライバシー保護と300秒待機テストが可能に。

## 背景
- REST APIはサードパーティ製（api-tn10.kaspa.org）
- TxIDやIPアドレスが外部に送信される
- 10秒後に自動的にREST APIフォールバックが発生し、300秒待機テストが不可能

## 主な変更内容

### 1. NetworkStateにrestApiControl追加
```javascript
restApiControl: {
    globalEnabled: true,     // 永続設定
    temporaryKilled: false,  // 一時無効化
    skipCount: 0,           // スキップ回数
    lastSkipped: null       // 最終スキップ時刻
}
```

### 2. REST APIポーリングにKillチェック追加
- 12476行目と12870行目の2箇所
- 無効化時はREST APIをスキップしてログ出力
- スキップ統計を自動更新

### 3. 設定タブにREST API設定セクション追加
- REST API有効/無効のトグル
- プライバシー警告メッセージ
- 状態とスキップ回数表示

### 4. デバッグタブにKillスイッチ追加
- 一時的な無効化/復活ボタン
- リアルタイム状態表示
- スキップ統計表示

### 5. DebugHandlers関数追加
- `toggleRestApiGlobal()`: グローバル設定変更
- `killRestApi()`: 一時無効化
- `reviveRestApi()`: 復活
- `updateRestApiStats()`: 統計更新

## 利点
1. **プライバシー保護**: 外部APIを完全に無効化可能
2. **300秒テスト**: 完全なMempool滞留シミュレーション
3. **柔軟な制御**: 永続設定と一時Kill
4. **企業対応**: コンプライアンス準拠

## テスト手順
1. ブラウザリロード
2. 設定タブでREST API無効化（永続）または
3. デバッグタブでKill（一時）
4. ブロックフィルター有効化
5. アップロード開始
6. 300秒待機の確認

## 次のステップ
- Phase 1-5の実装が可能に
- Mempool確認と対処機能の追加
# v5.12.3: DirectoryManager段階的削除完了

## 概要
古い未使用のDirectoryManagerクラスを段階的に削除。DirectoryBuilderStateが現在の実装。

## 削除内容

### Phase 1: 外部参照の無効化
- UIHandlers.switchTab内のdirectory判定削除（2箇所）
  - 6739-6742行目
  - 16868-16870行目

### Phase 2: インスタンス化の削除
- `window.directoryManager = new DirectoryManager();` 削除（16332行目）

### Phase 3: メソッドの段階的削除
- Phase 3-A: UIメソッド削除
  - updateUI()
  - updateParentDirectorySelector()
  - updateFileSelector()
- Phase 3-B: ダウンロード/エクスポートメソッド削除
  - downloadDirectory()
  - createDownloadFolder()
  - downloadFile()
  - exportDirectory()
- Phase 3-C: 残りのメソッド削除
  - DAG関連メソッド（loadDAGNodes, saveDAGNodes, createDAGNode, getDAGNode, traverseDAG）
  - ディレクトリ管理メソッド（loadDirectories, saveDirectories, getDirectory, deleteDirectory）
  - ユーティリティメソッド（generateCID, getDirectoryTree）

### Phase 4: クラス本体の削除
- DirectoryManagerクラス全体を削除（6970-7122行目）
- 153行のコード削減

## 影響
- メモリ使用量削減
- localStorage使用量削減（kaspaDirectories, kaspaDAG）
- コードサイズ削減（約5KB）
- 実行速度向上（不要なインスタンス化なし）

## 注意事項
- directory-tabは既に存在しないため、UI影響なし
- DirectoryBuilderStateが現在の主要実装として継続動作
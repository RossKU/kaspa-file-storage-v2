# v5.12.59: Info表示のサイズ修正

## 問題
- Infoボタンクリック時にファイルサイズが0Bと表示される
- CIDは短縮版（8文字）が表示される

## 原因
### テーブル表示とInfoボタンの不一致：
- **テーブル表示** (17915行目): `entry.fileSize`を参照 ✅
- **Infoボタン** (18516行目): `file.size`を参照 ❌
- CSV/KENVには`fileSize`として保存されている

## 修正内容

### showFileInfo関数のサイズ表示（18516行目）
```javascript
// 修正前
<strong>サイズ:</strong> ${formatSize(file.size)}<br>

// 修正後  
<strong>サイズ:</strong> ${formatSize(file.size || file.fileSize)}<br>
```

## データ参照の流れ
1. **ファイルタブのテーブル**:
   - `updateFileListFromKENV`関数（17855行目）
   - `FileState.kenvManager.entries`からデータ取得
   - `createKENVTableRow`で各行作成

2. **Infoボタン**:
   - `showFileInfo`関数（18470行目）
   - 優先順位：
     1. `FileState.kenvManager.entries`（KENV）
     2. `FileState.fileList`（フォールバック）

## 効果
- 既存データ（`fileSize`のみ）も正しく表示される
- 新規データ（`size`と`fileSize`両方）も対応
- CIDはv5.12.58で既にフルCID対応済み
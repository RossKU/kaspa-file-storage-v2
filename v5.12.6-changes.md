# v5.12.6: 完了メッセージ表示修正

## 概要
メタトランザクション作成時に完了メッセージが表示されない問題を修正。

## 問題
- メタTx作成時（デフォルト）は「トランザクションを確認しています...」のまま
- メタTxなしの場合のみ完了メッセージが表示されていた
- 10秒後に消えるが、完了が分かりづらい

## 原因分析
```javascript
// 12811行目: メタTxなしの場合のみ
if (!useMetaTx) {
    uploadStatusUI.updateStatus('✅', '完了！...', 5);
}

// メタTx作成の場合は完了メッセージがなかった
```

## 修正内容（13258-13259行目）
```javascript
// v5.12.6: Update status to complete for meta-tx case
uploadStatusUI.updateStatus('✅', 'アップロード完了！ファイルタブを確認してください', 5);
```

## 効果
- ✅ メタTx作成時も完了メッセージが表示される
- ✅ ユーザーが完了を確認できる
- ✅ 10秒後に自動で非表示（v5.12.4の機能が活きる）

## テスト確認事項
1. アップロード完了後「アップロード完了！」メッセージが表示される
2. 10秒後に自動で非表示になる
3. メタTxあり/なし両方で動作する
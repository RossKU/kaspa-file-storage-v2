# v5.7.1 変更内容

## 修正内容
WebSocket切断イベントが監視停止後も出続ける問題を修正しました。

### 変更箇所
1. **disconnectイベントハンドラー（11345-11360行目）**
   - `monitorActive`フラグのチェックを追加
   - 監視停止後はイベントを無視

### 変更前
```javascript
NetworkState.rpcClient.addEventListener('disconnect', (event) => {
    log('[WebSocket] 切断イベントを検出', 'warning');
    NetworkState.ws.connected = false;
    // ...
});
```

### 変更後
```javascript
NetworkState.rpcClient.addEventListener('disconnect', (event) => {
    // v5.7.1: Only process disconnect events while monitoring is active
    if (!NetworkState.ws.monitorActive) {
        return; // Ignore disconnect events after monitoring stopped
    }
    
    log('[WebSocket] 切断イベントを検出', 'warning');
    NetworkState.ws.connected = false;
    // ...
});
```

## 解決される問題
1. アップロード完了後も「[WebSocket] 切断イベントを検出」が出続ける
2. 約60-80秒ごとに切断ログが繰り返される
3. 不要なエラーログによる混乱

## テスト項目
1. アップロード完了後、切断イベントログが出ないこと
2. 監視中は正常に切断イベントを検出すること
3. 再度アップロードした際は正常に動作すること

## 変更規模
- **最小限**: 4行の追加のみ
- **影響範囲**: disconnectイベント処理のみ
- **リスク**: 極めて低い
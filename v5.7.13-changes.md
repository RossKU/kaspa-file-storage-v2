# v5.7.13 変更内容

## 修正内容
kaspa-core WASMの初期化方法を新しい形式に更新し、deprecated parameters警告を解消しました。

### 問題
```
using deprecated parameters for the initialization function; pass a single object instead
```

### 原因
kaspa-core WASMライブラリの初期化で古い形式（文字列パラメータ）を使用していました。

### 解決方法
WASM初期化を新しいオブジェクト形式に更新しました。

#### 修正前（6310行目）
```javascript
// Initialize WASM binary
await NetworkState.kaspa.default('./kaspa-core_bg.wasm');
```

#### 修正後
```javascript
// Initialize WASM binary
// v5.7.13: Use new initialization format to avoid deprecated parameters warning
await NetworkState.kaspa.default({ module_or_path: './kaspa-core_bg.wasm' });
```

## 影響
- **警告の解消**: deprecated parameters警告が表示されなくなります
- **将来の互換性**: 新しいAPI形式に準拠し、将来的な互換性が向上
- **動作の変更**: なし（機能的には同じ）

## その他の注意事項
- GroupMarkerNotSetエラーはChromeの内部WebGLエラーで、アプリケーション側では対処不要
- WebGL自動フォールバックの警告も同様にブラウザ側の問題

## テスト項目
- [ ] システム初期化が正常に完了すること
- [ ] コンソールにdeprecated parameters警告が表示されないこと
- [ ] WASMバイナリが正しく読み込まれること
- [ ] 全ての機能が正常に動作すること
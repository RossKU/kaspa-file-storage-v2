# v5.7.3 変更内容

## 修正内容
イベントリスナーの蓄積によるメモリリーク問題を修正しました。

### 変更箇所
1. **stopMonitoring関数（11463-11465行目）**
   - `removeAllEventListeners()`を追加
   - すべてのイベントリスナーを一括削除
   - デバッグログを追加

### 変更前
```javascript
await NetworkState.rpcClient.unsubscribeBlockAdded();
NetworkState.ws.connected = false;  // v5.6.4: Mark WebSocket as disconnected
```

### 変更後
```javascript
await NetworkState.rpcClient.unsubscribeBlockAdded();

// v5.7.3: Remove all event listeners to prevent memory leak
NetworkState.rpcClient.removeAllEventListeners();
log('[DEBUG] イベントリスナーをクリアしました', 'debug');

NetworkState.ws.connected = false;  // v5.6.4: Mark WebSocket as disconnected
```

## 解決される問題
1. **メモリリーク**: 監視の開始・停止を繰り返してもリスナーが蓄積しない
2. **パフォーマンス低下**: 不要なイベント処理が実行されない
3. **将来的な改善**: v5.7.1の回避策が不要になる（ただし安全のため残す）

## テスト項目
1. アップロード → 監視停止が正常に動作すること
2. 複数回のアップロード → リスナーが蓄積しないこと
3. WebSocket切断時 → 重複ログが出ないこと
4. エラー発生時 → 適切にエラーハンドリングされること

## 変更規模
- **最小限**: 3行の追加のみ
- **影響範囲**: stopMonitoring関数内のみ
- **リスク**: 極めて低い（try-catch内で実行）

## 技術的詳細
- `removeAllEventListeners()`はkaspa-core.jsのRpcClientクラスが提供するメソッド
- 'disconnect'と'block-added'を含むすべてのイベントリスナーを削除
- 次回の監視開始時に新しいリスナーが登録される
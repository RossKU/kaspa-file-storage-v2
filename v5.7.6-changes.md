# v5.7.6 変更内容

## 修正内容
handleError関数でRPCクライアントの破棄処理が条件によりスキップされる問題を修正しました。

### 変更箇所
1. **ConnectionManager.handleError関数（3733-3743行目）**
   - RPCクライアント破棄処理を`if (this.isConnected)`の外に移動
   - 接続状態に関わらず、常にRPCクライアントを破棄

### 変更前
```javascript
if (this.isConnected) {
    this.isConnected = false;
    
    // v5.7.5: Cleanup RPC client on network error
    if (NetworkState.rpcClient) {
        // この処理がisConnected=falseの場合スキップされていた
    }
}
```

### 変更後
```javascript
// v5.7.6: Always cleanup RPC client first, regardless of connection state
if (NetworkState.rpcClient) {
    try {
        await NetworkState.rpcClient.disconnect();
        log('[ConnectionManager] RPC接続を切断しました', 'info');
    } catch (disconnectError) {
        log(`[ConnectionManager] RPC切断エラー: ${disconnectError.message}`, 'debug');
    }
    NetworkState.rpcClient = null;
    log('[ConnectionManager] RPCクライアントをクリアしました', 'debug');
}

// Mark as disconnected
if (this.isConnected) {
    // 状態更新処理
}
```

## 解決される問題

### 問題のシナリオ
1. WebSocket切断イベントが発生 → `ConnectionManager.isConnected = false`
2. その後、handleErrorが呼ばれる
3. `if (this.isConnected)`がfalseのため、RPCクライアント破棄がスキップ
4. RPCクライアントがリークする

### 修正後の動作
- 接続状態に関わらず、ネットワークエラー時は必ずRPCクライアントを破棄
- 複数の切断イベントが発生しても、確実にクリーンアップ

## 期待されるログ
```
[ConnectionManager] ネットワークエラーを検出
[ConnectionManager] RPC接続を切断しました
[ConnectionManager] RPCクライアントをクリアしました
```

これらのログが、isConnectedの状態に関わらず出力されます。

## テスト項目
1. WebSocket切断後のhandleError呼び出しでRPCクライアントが破棄されること
2. 複数回の切断イベントでもリークが発生しないこと
3. 通常のエラー処理が影響を受けないこと

## 変更規模
- **最小限**: 10行程度の移動のみ
- **影響範囲**: handleError関数内のみ
- **リスク**: 極めて低い（処理順序の変更のみ）
# v5.7.7 タイミングと競合状態の詳細分析

## 発見された競合状態

### 1. 再接続試行の重複

#### タイムライン詳細（ミリ秒単位）
```
22:06:58.865 - [WebSocket] 切断イベントを検出
22:06:58.866 - [ConnectionManager] 2000ms後に再接続を試行します（1）
22:07:00.353 - [DEBUG] ネットワーク切断を検出 - BlockID待機を中断
22:07:00.355 - [ConnectionManager] 再接続試行 #1（scheduleReconnectから）
22:07:00.355 - [DEBUG] 再接続を待機中...（processAndUploadから）
22:07:00.867 - [ConnectionManager] 既に再接続中です（512ms後）
```

#### 分析
- **2つの再接続トリガー**が存在：
  1. WebSocket disconnectハンドラー → scheduleReconnect（2秒タイマー）
  2. processAndUploadのエラー処理 → 即座の再接続試行

- **512msの遅延**で「既に再接続中」が発生
  - これは異なるイベントループのタイミングを示唆
  - 再接続ロックは機能しているが、複数のトリガーが存在

### 2. 切断検出の遅延問題

#### 観察された遅延
```
実際の切断: 22:06:58.865
processAndUploadでの検出: 22:07:00.353
遅延: 1488ms
```

#### 遅延の原因
```javascript
// BlockID待機ループ（推測）
while (!blockFound && Date.now() - startTime < 30000) {
    if (!ConnectionManager.isConnected || !NetworkState.ws.connected) {
        throw new Error('Network disconnected while waiting for BlockID');
    }
    await sleep(100); // ← この間隔で切断をチェック
}
```

### 3. トランザクション処理の競合

#### タイムライン
```
切断前:
- トランザクション送信（TxID: 39a1ee...）
- BlockID待機開始

切断中（1.5秒）:
- トランザクションがブロックに含まれる
- しかしクライアントは切断のため検出できず

再接続後:
- 同じペイロードで再送信試行
- 「output already spent」エラー
```

## 競合状態の根本原因

### 1. 非同期処理の協調不足
```
WebSocketレイヤー     ：即座に切断を検出
ConnectionManager    ：2秒後に再接続
processAndUpload     ：100ms間隔で切断をチェック
トランザクション処理   ：独立して進行
```

### 2. 状態管理の分散
- `ConnectionManager.isConnected`
- `NetworkState.ws.connected`
- `NetworkState.rpcClient`
- トランザクション送信状態

これらが独立して管理され、同期が取れていない。

### 3. エラーリカバリーの重複
```javascript
// 複数の場所でエラーハンドリング
1. WebSocket disconnectハンドラー
2. ConnectionManager.handleError
3. processAndUploadのcatchブロック
4. 各種タイムアウトハンドラー
```

## 影響と症状

### 1. パフォーマンスへの影響
- 不要な再接続試行
- 重複したエラーハンドリング
- リソースの無駄遣い

### 2. データ整合性のリスク
- トランザクションの二重送信
- 状態の不整合
- 予期しないエラー

### 3. ユーザー体験への影響
- エラーメッセージの混乱
- 処理の遅延
- 不安定な動作

## 技術的提案

### 1. 切断検出の高速化
```javascript
// 現在: 100ms間隔
await sleep(100);

// 提案: 50ms間隔 + 即座のチェック
await sleep(50);
```

### 2. 再接続トリガーの一元化
```javascript
// すべての再接続要求を単一のキューで管理
const ReconnectionQueue = {
    pending: false,
    queue: [],
    
    request(source) {
        if (this.pending) return;
        this.pending = true;
        // 単一の再接続処理
    }
}
```

### 3. トランザクション状態の追跡
```javascript
const TransactionTracker = {
    pending: new Map(), // TxID -> 状態
    
    isPending(txId) {
        return this.pending.has(txId);
    },
    
    markSent(txId, payload) {
        this.pending.set(txId, { payload, timestamp: Date.now() });
    }
}
```

## 結論

v5.7.7で再接続機能は復旧したが、複数の非同期処理が独立して動作し、競合状態を引き起こしている。特に：

1. **再接続の重複トリガー**は無害だが非効率
2. **切断検出の遅延**がトランザクション処理の問題を引き起こす
3. **状態管理の分散**が根本的な問題

これらの問題は、システム全体のアーキテクチャレベルでの改善が必要であることを示している。ただし、「マジでちょっとずつ」のアプローチに従い、まずは最小限の改善から始めるべきである。
# v5.9.1 変更内容

## 概要
v5.9.0の機能に対して、UIのみの実装を追加しました。実際の機能は後で実装予定です。

## 主な変更点

### 1. 共有メニューのドロップダウンUI

#### 追加されたUI要素
- Shareボタンクリックで表示されるドロップダウンメニュー
- 5つの共有オプション:
  - 🔐 .kaspa (パスワード付き)
  - 🔓 .kaspa (パスワードなし)
  - 🔐 TxID (パスワード付き)
  - 🔓 TxID (パスワードなし)
  - 🔑 Pass (パスワードのみ)

#### CSS追加
- `.share-dropdown`: ドロップダウンコンテナ
- `.share-menu`: メニュー本体
- `.share-option`: 各オプションのスタイル

#### JavaScript関数追加
- `toggleShareMenu()`: メニューの開閉
- `closeShareMenu()`: 外部クリックで閉じる
- `shareAction(action)`: 各アクションの処理（UIのみ）

### 2. パスワード入力ダイアログ

#### 2つのシナリオで表示
1. ファイル追加時にパスワードなしの場合
2. 共有メニューでパスワード付きオプション選択時

#### CSS追加
- `.password-dialog`: ダイアログ本体
- `.dialog-overlay`: 背景オーバーレイ
- `.password-dialog-buttons`: ボタンエリア

#### JavaScript関数追加
- `showPasswordDialog()`: 共有用パスワードダイアログ
- `showAddFilePasswordDialog()`: ファイル追加用パスワードダイアログ
- `closePasswordDialog()`: ダイアログを閉じる
- `submitPassword()`: パスワード送信（UIのみ）
- `confirmAddFile()`: ファイル追加確認（UIのみ）

### 3. Archive Node接続表示の拡張

#### 追加された表示要素
- 接続数表示: "接続数: 3/5"
- サーバー情報エリア（接続時のみ表示）:
  - Server: wss://archive-mainnet.kaspa.org
  - Version: rusty-kaspa/0.15.2
  - Blocks: 85.3M

#### JavaScript関数追加
- `updateArchiveNodeDisplay()`: ダミーの表示更新関数（ランダムで接続/切断を切り替え）

### 4. ファイル追加時のパスワード処理

#### 動作
- TxID:BlockID形式でパスワードがない場合、自動的にパスワード入力ダイアログを表示
- パスワードは省略可能
- 登録ボタンでファイルリストに追加

## 技術的詳細

### UIのみの実装
すべての新機能は見た目のみで、実際の処理は含まれていません：
- ログメッセージに "(UI only)" を追加
- 実際のネットワーク通信なし
- ファイルのダウンロード/アップロードなし
- 暗号化/復号化処理なし

### 今後の実装予定
1. Archive Node接続の実装
2. 共有機能の実装（.kaspaファイル生成、クリップボードコピー）
3. パスワード検証とファイル暗号化
4. 自動登録機能の実装

## テスト項目
- [x] 共有メニューの開閉
- [x] パスワードダイアログの表示/非表示
- [x] Archive Node表示の切り替え
- [x] ファイル追加時のパスワードプロンプト
- [x] 各UIコンポーネントのレスポンシブデザイン
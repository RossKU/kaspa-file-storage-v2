# v6.0.5: UI English Translation - Phase 2 Complete

## Changes
- Translated 35+ button texts and UI elements from Japanese to English
- Version update: v6.0.4 → v6.0.5
- Phase 2 of UI internationalization completed

## Button Texts Translated (18 items)
- メタTxIDから復元 → "Restore from MetaTxID"
- 新規作成 → "Create New"
- 選択中の場所に新規フォルダを作成 → "Create New Folder Here"
- 生成のみ → "Generate Only"
- 更新 → "Update"
- すべて → "All"
- .kaspa (パスワード付き) → ".kaspa (with password)"
- .kaspa (パスワードなし) → ".kaspa (no password)"
- TxID (パスワード付き) → "TxID (with password)"
- TxID (パスワードなし) → "TxID (no password)"
- Pass (パスワードのみ) → "Pass (password only)"

## Labels and Placeholders (9 items)
- パスワード → "Password"
- ディレクトリビルダー → "Directory Builder"
- ディレクトリ名 → "Directory Name"
- ディレクトリ構造 → "Directory Structure"
- ディレクトリ → "Directories"
- パスワード保護 (必須) → "Password Protection (Required)"
- ダウンロード設定 → "Download Settings"
- 並列ダウンロード (実験的機能) → "Parallel Download (Experimental)"
- テスト用ブロックハッシュ (任意) → "Test block hash (optional)"

## Log Messages (17 items)
- ログをクリアしました → "Log cleared"
- RPCクライアントをクリアしました → "RPC client cleared"
- 送金額を更新しました → "Transaction amount updated"
- .kprogressファイルを削除しました → ".kprogress file deleted"
- 最近削除した項目 → "Recently Deleted"
- メタデータをクリアしました → "Metadata cleared"
- アーカイブノードステータスを更新しました → "Archive node status updated"
- エントリー追加 → "Entry added"
- LocalStorageをクリアしました → "LocalStorage cleared"
- ファイルを追加しました → "File added"
- ディレクトリを追加しました → "Directory added"
- 少なくとも1つのエントリーを追加してください → "Please add at least one entry"
- 統計リセット → "Statistics reset"
- Kaspaシステム再起動を開始 → "Starting Kaspa system restart"
- すべての必要な関数を確認しました → "All required functions verified"
- アップロード開始前残高 → "Balance before upload"
- 初期化開始 → "Initialization started"

## Test/Debug Messages
- Connectテスト → "Connection test"
- Connectテスト成功 → "Connection test successful"
- Connectテスト失敗 → "Connection test failed"
- リセット完了 → "Reset complete"
- REST APIテストモード → "REST API Test Mode"
- 複数WebSocketテスト → "Multiple WebSocket Test"

## Summary
Total: 35+ translations completed
Phase 2 of v6.0.x UI internationalization successfully completed
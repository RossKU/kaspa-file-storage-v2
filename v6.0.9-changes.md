# v6.0.9: UI English Translation - Phase 6 Complete

## Changes
- Translated 40+ UI elements and labels from Japanese to English
- Version update: v6.0.8 → v6.0.9
- Phase 6 of UI internationalization completed

## Main UI Elements (15 items)
- ネットワークConnect状況 → "Network Connection Status"
- 容量 → "Size"
- UP日 → "Date"
- またはドラッグ&ドロップ → "or drag & drop"
- インポート → "Import"
- TxIDから読込 → "Load from TxID"
- .kaspaから読込 → "Load from .kaspa"
- 合計サイズ → "Total Size"
- テーマ → "Theme"
- 秘密鍵 → "Private Key"
- ネットワーク → "Network"
- ノードタイプ → "Node Type"

## Theme & Options (10 items)
- ライト / ダーク → "Light / Dark"
- Mainnet (本番) → "Mainnet (Production)"
- 自動選択 (推奨) → "Auto Select (Recommended)"
- 通常ノード → "Standard Node"
- アーカイブノード → "Archive Node"
- カスタム → "Custom"
- カスタムRPCエンドポイント → "Custom RPC Endpoint"

## Settings & Features (15 items)
- REST API設定 → "REST API Settings"
- リトライ回数 → "Retry Count"
- アーカイブノード探査 → "Archive Node Discovery"
- Kaspaシステム再起動 → "Kaspa System Restart"
- Upload設定 → "Upload Settings"
- チャンクサイズ → "Chunk Size"
- 20KB chunks (実験的・旧方式) → "20KB chunks (Experimental - Legacy)"
- 22KB chunks (推奨・新方式) → "22KB chunks (Recommended - New)"
- メタTxIDを使用（推奨：履歴から簡単に共有可能） → "Use MetaTxID (Recommended: Easy to share from history)"
- ランダムCIDを使用（検閲回避） → "Use Random CID (Censorship Resistance)"
- 対象ノード（空 = 全ノード） → "Target Nodes (empty = all nodes)"
- ブロックイベントフィルター → "Block Event Filter"
- デバッグログ → "Debug Log"

## Additional UI Text
- Connect数 → "Connections"
- アプリケーションの外観を切り替えます → "Switch application appearance"
- 対応フォーマット → "Supported Formats"
- 作業フォルダを選択 → "Select Workspace"
- アーカイブノード強制使用 → "Force Archive Node"
- アーカイブノードを探索中 → "Searching for archive nodes"
- 探索 → "Search"
- 閾値 → "Threshold"
- TxID:BlockID形式（推奨、REST API不要） → "TxID:BlockID format (Recommended, no REST API needed)"
- TxIDのみ（REST API必要） → "TxID only (REST API required)"
- 自動的にメタデータタイプを判別します → "Automatically detects metadata type"

## Summary
Total: 40+ UI translations completed
Phase 6 of v6.0.x UI internationalization successfully completed
# v6.1.2 Changes - ダウンロードタブ切り替え修正

## 🔧 Fixed Issues

### ダウンロードタブへの切り替え修正
- **問題**: FileタブのDownloadボタンをクリックしても、ダウンロードタブに切り替わらない
- **原因**: 間違ったセレクタ`[data-tab="download"]`を使用していた
- **修正**: `UIHandlers.switchTab('download')`を直接呼び出すように変更

## 📝 Technical Details

### 修正箇所
- 17719-17728行目

### 修正内容
```javascript
// 修正前
const downloadTab = document.querySelector('[data-tab="download"]');
if (downloadTab) {
    downloadTab.click();
}

// 修正後
if (typeof UIHandlers !== 'undefined' && UIHandlers.switchTab) {
    UIHandlers.switchTab('download');
} else {
    // Fallback: find and click the download tab element
    const downloadTab = document.querySelector('.nav-item[onclick*="download"]');
    if (downloadTab) {
        downloadTab.click();
    }
}
```

## 🎯 Benefits
- ダウンロードタブへの自動切り替えが正常に動作
- フォールバック処理も追加してより確実に

## 📅 Release Date
- 2025-08-17
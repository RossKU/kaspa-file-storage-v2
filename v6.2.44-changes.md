# v6.2.44: Fix KENV import functionality

## Fixed
- **KENV Import Not Working**: Fixed issue where imported entries were not displayed in File tab
  - Added missing `updateIndex()` call to update sort indexes after import
  - Fixed incorrect function name: `updateFileList` → `updateFileListFromKENV`
  - Now imported entries are properly indexed and displayed in the UI

## Problem Details
- Import reported "4 added" but entries didn't appear in File tab
- Root cause: Sort indexes were not updated, and wrong UI update function was called
- Solution: Update indexes for each imported entry and call correct UI update function

## Impact
- KENV import/export feature now works correctly
- File tab updates immediately after import